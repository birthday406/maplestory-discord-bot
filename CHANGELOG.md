# 변경 이력

## 2026-09-15

### 변경됨 — 관리자 설정을 홈페이지로 통합 (미배포)
- `/채널설정`을 전역 등록 목록에서 제외하고 `/관리자`는 홈페이지 로그인·서버 선택·설정 저장 안내와 대시보드 버튼을 본인에게만 표시합니다.
- 기존 설정과 홈페이지 공통 처리 함수는 유지하고 홈페이지 명령어 소개·사용 흐름도 변경했습니다.

### 수정됨 — 마이너 패치 점검 중복 소개 (미배포)
- 운영 기록에서 점검 배너와 11초 뒤 본문이 각각 전송된 사례를 확인했습니다. 이미 안내한 공식 점검 링크와 예정 시각만 담긴 정형 소개를 중복으로 판정하도록 보완했습니다.
- 같은 작성자가 60초 안에 올린 앞선 배너에도 동일한 판정을 적용합니다. 연장·완료·장애 등 추가 내용과 미안내 원문은 유지하며 관련 테스트 19개를 통과했습니다.

### 변경됨 — 홈페이지 명령어 안내·날짜별 업데이트 (미배포)
- 상단 샤벳 로고를 소개 페이지로 연결하고 명령어에 상세 설명·사용 순서·주의점을 추가했습니다.
- 랭킹·HEXA·채널설정의 사용 흐름을 재생하는 예시를 추가했습니다. 실제 녹화가 아닌 재현 예시로 표시하며 동작 줄이기 설정을 존중합니다.
- 같은 날짜의 공개 업데이트는 한 카드로 묶고 기존 변경 내역은 모두 보존합니다.

### 수정됨 — 공식 월드 랭킹 기준 (미배포)
- 이름·경험치로 만든 순서를 제거하고 공식 월드 순위 숫자와 순서를 그대로 표시합니다. 개인 조회의 전체 순위와 월드 순위를 별도로 보관합니다.
- 관련 테스트 72개 통과. 공식 Kronos 상위 10명의 순서를 로컬 화면에서 확인했습니다. 운영 배포 전 DB 백업과 보존된 월드 수집 결과를 이용한 순위 초기화가 필요합니다.

### 수정됨 — 랭킹 숫자·사진 배치 (미배포)
- 순위 숫자 뒤에 캐릭터 사진이 오도록 순서를 바로잡고 사진을 PC 96px, 모바일 80px 너비로 키웠습니다.

### 배포 완료 — 랭킹 상세·모바일 화면
- `e209f66`을 커밋·푸시하고 변경 파일 9개를 서버에 적용했습니다. 로컬·서버 테스트 각 54개 통과 후 2026-09-16 02:26:26 UTC 재시작했습니다.
- 홈페이지 HTTP 200, OAuth 준비·Discord 재연결 정상, 시작 오류 0건과 공개 페이지의 사진·상단 날짜·캐릭터 상세 이동을 확인했습니다. 아래 미배포 화면 개선을 운영에 반영했습니다.

### 변경됨 — 랭킹 상세·모바일 화면 (미배포)
- 랭킹 날짜를 목록 상단으로 모으고 사진·이름에서 별도 캐릭터 상세 페이지로 이동하도록 개선했습니다.
- 상세에 저장된 순위·유니온·업적·경험치를 표시하고 홈 버튼 간격·화살표와 모바일 배치를 정리했습니다.

### 배포 완료 — 웹 랭킹·판매 기간·안내 개선
- `3aeab56` 커밋·푸시 후 변경 파일 22개를 운영에 적용했습니다. 로컬 테스트 116개와 서버 홈페이지 테스트 54개를 통과했습니다.
- 운영 DB 온라인 백업을 별도 파일로 만들고 전체 무결성·851,549명 캐릭터·SHA-256을 검증한 뒤 배포했습니다.
- 2026-09-16 02:02:51 UTC 재시작 후 홈페이지 HTTP 200, OAuth 준비 정상, Discord 재연결, 시작 오류 0건을 확인했습니다. 첫 인덱스 생성 중 잠시 502가 있었고 시작 완료 후 정상화됐습니다.
- 실제 공개 화면의 캐릭터 검색·4개 월드별 100명 조회와 판매 기간·간소화한 업데이트 내역을 확인했습니다. 아래 미배포 기록의 변경은 이번에 모두 적용됐으며 기존 게시된 공지를 일괄 수정하지는 않았습니다.

### 변경됨 — 공개 업데이트 내역 간소화 (미배포)
- 홈페이지 업데이트 내역을 짧은 제목과 핵심 변경 2~3줄로 정리하고 앞으로도 같은 기준을 적용합니다.

### 변경됨 — 항해·도핑 안내 색상 (미배포)
- `/항해`와 `/도핑`의 기존 안내 이미지를 샤벳 홈페이지와 어울리는 차콜 배경·분홍 제목·연보라 강조색으로 변경했습니다.
- 이미지 생성 대신 원본 픽셀의 색만 치환했습니다. 문구·배치·해상도는 유지하고 도핑 아이콘과 조건별 파랑·보라·초록 표시는 원본 그대로 보존했습니다.
- 기존 이미지 전송 테스트 2개와 보호 영역 픽셀 일치 검사를 통과했습니다. 운영 서버에는 아직 적용하지 않았습니다.

### 추가됨 — 기간 한정 시뮬레이터와 웹 랭킹 (미배포)
- 프리렌 시그니처·Heroic 원더베리에 판매 시작·종료를 표시합니다. 시작 전과 2026-10-08 00:00 UTC 이후에는 진행 중이 아니라는 안내로 대체하며 기존 다시 뽑기 버튼도 차단합니다. 시작은 9월 9일 점검 완료 공지 시각(20:08 UTC)을 기준으로 명시합니다.
- 홈페이지에 로그인 없는 캐릭터 검색과 4개 수집 월드의 경험치 상위 100명 표를 추가했습니다. 공개 게임 기록만 읽고 Discord 사용자별 조회 이력은 노출하거나 변경하지 않습니다. 캐릭터별 UTC 기준일·다음 레벨까지 경험치·유니온·업적과 최근 기록 간 획득 경험치를 보여줍니다.
- 월드별 정렬 인덱스와 1분 캐시, 조회 시간 제한·IP별 분당 30회 제한을 추가했습니다. 운영 배포 전에는 규칙에 따라 수동 DB 백업이 필요합니다.
- 공식 Discord 소식의 `공식 announcements · 표시 시각 UTC` 하단 문구를 제거했습니다. 번역 대기 중에는 `번역 준비 중`만 표시합니다.
- 관련 테스트 94개와 수집기 테스트 16개를 통과했습니다. 읽기 전용으로 가져온 공개 표본 400개로 로컬 목록·캐릭터 상세 화면을 확인했고, 승인된 test 채널에 기간·종료 후·하단 제거 정적 예시를 게시했습니다. 운영 DB 인덱스 생성·기존 공지 수정·서버 배포는 아직 하지 않았습니다.

### 배포 확인 — 홈페이지 공개 준비와 실제 저장 검증
- `8132b0a`를 커밋·푸시하고 변경 파일 14개를 운영 서버 기존 해시와 대조한 뒤 반영했습니다. 로컬 관련 테스트 62개, 서버 홈페이지 테스트 50개를 통과했습니다.
- 2026-09-16 01:04:27 UTC 재시작 후 홈페이지 HTTP 200, OAuth 준비 정상, Discord 재연결과 시작 오류 0건을 확인했습니다.
- 실제 Discord 로그인 후 에무새의 새장 `test`의 공지 알림을 홈페이지에서 ON으로 저장하고 Discord `/채널설정`에서도 같은 ON 상태가 표시되는지 확인했습니다. 테스트 뒤 OFF로 복원했습니다. 다른 채널 설정은 변경하지 않았습니다.
- Codex에서 홈페이지를 30분마다 확인하고 반복 확인에도 실패하거나 복구된 경우만 알리도록 보조 감시를 등록했습니다. Codex 호스트 실행 상태에 의존합니다.

### 추가됨 — 홈페이지 공개 준비 보완
- 초대 버튼을 Discord에 설정된 공식 설치 링크로 연결하고 상단에 로그인한 사용자 이름을 표시합니다.
- 명령어 안내에 기존 시그니처·원더베리 실제 결과 이미지를 넣고 준비되지 않은 빈 이미지 상자는 표시하지 않습니다.
- 로그인 시작은 IP별 분당 10회, 나머지 인증·설정 API는 분당 120회로 제한하며 초과 시 재시도 시간을 안내합니다. 요청 기록은 메모리에만 유지하고 만료 후 다음 요청에서 정리합니다.
- Discord 개발자 페이지에 공개 이용약관·개인정보처리방침 URL을 등록했습니다.

### 수정됨 — 홈페이지 Discord 로그인 버튼
- 로그인 링크에 배경색만 붙어 형광펜처럼 보이던 표시를 수정했습니다. 충분한 클릭 영역·둥근 모서리·테두리·호버 상태가 있는 버튼으로 표시합니다.

### 추가됨 — 홈페이지 업데이트 내역
- 상단·하단 메뉴에 로그인 없이 읽는 업데이트 내역을 추가하고 최근 실제 배포된 홈페이지·캐시샵 공지·채널 설정·응답 가독성 개선을 정리했습니다.
- 공개 변경 목록은 `website/dist/updates.json`에 최신순으로 관리하며, 앞으로 사용자 영향이 있는 배포 시 함께 갱신하도록 프로젝트 규칙에 기록했습니다.

### 수정됨 — 공개 대시보드의 실제 설정 연결
- 대시보드 메뉴와 기존 `#dashboard` 주소가 가상 시안을 열던 연결을 실제 Discord 로그인·관리 서버 설정 화면으로 수정했습니다.
- 가상 시안은 별도 `#preview` 주소에만 유지하며 기본 화면은 샤벳 소개로 변경했습니다. 소개·명령어·정책 페이지에서 가상 시안 배너가 표시되지 않도록 했습니다.

### 배포 확인 — 남은 캐시샵 개선·공개 정책 안내
- `5b5121d`의 캐시샵 소개 중복 방지, 신규 판매 요약 범위, 번역 용어, 약관·개인정보 안내와 홈페이지 문의 정렬을 운영 서버에 반영했습니다. 아래 같은 날짜의 배포 전 기록에 해당하는 변경은 이제 배포 완료입니다.
- 로컬·서버에서 관련 테스트 75개를 통과했고, 2026-09-16 00:41:16 UTC 봇 재시작 후 Discord 재연결·시작 오류 0건·공개 홈페이지 HTTP 200·정책 데이터 응답을 확인했습니다. 공개 정책 화면도 브라우저에서 확인했습니다.
- 이번 공개 경로에 맞춰 Cloudflare의 홈페이지 요청 전달·보안 처리도 개인정보 안내에 반영했습니다.

### 추가됨 — 샤벳 공개 도메인 연결
- 메인 서버에 공식 `cloudflared 2026.9.1`과 홈페이지 전용 Tunnel 서비스를 설치하고 `https://sherbetbot.com`을 내부 홈페이지에 연결했습니다. 홈페이지 포트는 계속 로컬에만 바인딩합니다.
- 공개 주소 설정 적용·봇 재시작 후 HTTPS 홈페이지 및 로그인 준비 API 응답, Discord 재연결, 시작 오류 없음까지 확인했습니다. Discord 개발자 페이지에 공개 콜백도 등록했습니다. 운영자가 공개 주소에서 로그인 완료를 확인했습니다. 서버 목록 및 설정 저장의 최종 사용자 검증은 별도입니다.

### 수정됨 — 홈페이지 문의 링크 정렬 (운영 배포 전)
- 하단 문의 버튼을 메일 링크로 바꿔 다른 메뉴와 같은 높이로 표시합니다. 버튼 기본 안쪽 여백 때문에 글자가 내려가던 문제를 수정했습니다.

### 추가됨 — 샤벳 약관·개인정보 안내 (운영 배포 전)
- 봇 `/명령어` 도움말에 본인에게만 표시되는 이용약관·개인정보처리방침 버튼을 추가했습니다. 홈페이지 하단에도 로그인 없이 읽을 수 있는 같은 문서를 연결했습니다.
- 운영 문의·삭제 요청 이메일을 `valentineday302@gmail.com`으로 연결하고 비공식 팬 서비스 안내를 추가했습니다. 사업자 정보나 미확정 도메인은 기재하지 않았습니다.
- 실제 로그인 세션, 최근 캐릭터 조회, 서버 설정, 공개 자료·번역 API 처리와 보관 방식을 문서화하고 수동 문의 처리 절차를 정리했습니다.

### 변경됨 — 캐시샵 용어사전 (운영 배포 전)
- `Royal Styles`는 `로얄 스타일`, `Prism Color Restore`는 `프리즘 컬러 리스토어`로 지정했습니다.

### 수정됨 — 캐시샵 업데이트 요약 범위 (운영 배포 전)
- 주간 Cash Shop Update는 공식 `ONGOING SALES`·`SALES ENDING THIS WEEK` 구분 앞의 신규 업데이트와 Daily Deals만 GPT 요약에 전달합니다. 계속 판매 중인 제네시스 패스 등을 새 판매처럼 반복 안내하지 않습니다.
- 요약 항목 수를 억지로 채우지 않고, 원문 근거 없이 판매를 할인·재판매로 표현하지 않도록 했습니다. 구분을 찾지 못하면 전체 목록을 대신 요약하지 않고 오류로 남깁니다.
- 9월 16일 공식 원문에서 로얄 스타일·프리즘 판매 유지와 제네시스 패스·기존 스타 스탬프 제외를 검증했습니다.

### 수정됨 — 공식 Discord 캐시샵 소개 중복 (운영 배포 전)
- 이미 전송한 캐시샵 글의 `Take a look ... featuring Royal Styles and more!` 소개도 강조·인사 이모지와 관계없이 중복으로 제외합니다. 추가 문장이 있는 글은 보존합니다.
- 이미지 단독 공지는 최대 60초 기다린 뒤, 바로 다음 메시지가 같은 작성자의 60초 이내 중복 링크 소개인 경우 함께 제외합니다. 그 외 이미지와 일반 본문·긴급 공지는 기존 전송을 유지합니다.
- 실제 원문 재현과 이미지 대기·재시작·독립 공지 보존을 포함해 관련 테스트 24개를 통과했습니다. 이미 전송된 메시지는 변경하지 않았습니다.

### 배포 확인 — 공개 주소 준비 코드
- `17145a0`까지 서버 반영 후 관련 검사 65개를 통과했습니다. 2026-09-15 22:02:37 UTC 재시작, Discord 재접속·홈페이지 HTTP 200·시작 이후 오류 0건을 확인했습니다.
- 현재는 로컬/SSH 터널 모드이며 공개 도메인·DNS·HTTPS 적용과 운영자 로그인 후 실제 저장 검증이 남아 있습니다.

### 배포됨 — 홈페이지 설정 저장
- `a88a7bd`를 GitHub와 메인 서버에 반영했습니다. 서버 관련 검사 62개 통과, 2026-09-15 21:56:56 UTC 재시작, Discord 연결·HTTP 200·시작 이후 오류 0건을 확인했습니다. 실제 저장 E2E는 운영자 재로그인을 기다립니다.

### 추가됨 — 공개 주소 연결 준비
- 환경변수의 HTTPS 주소로 Host·OAuth 콜백·Origin·Secure 쿠키를 함께 전환하도록 준비했습니다. 기본 로컬 동작을 유지하며 도메인·DNS·인증서는 아직 적용하지 않았습니다.
- 실제 계정 설정 화면과 가상 디자인 화면의 상단 안내를 구분했습니다.


### 변경됨 — 홈페이지 기능별 채널 선택 (운영 배포 전)
- 공지·썬데이 등 각 기능 카드에서 채널을 개별 선택하도록 변경하고, 현재 사용 중인 채널을 표시합니다.
- 추가 확인 단계를 없애 저장 버튼 한 번으로 적용합니다. 다른 카드의 미저장 입력은 유지하며 선택한 채널의 해당 기능만 변경합니다.

### 추가됨 — 홈페이지 나머지 채널 설정 (운영 배포 전)
- 썬데이 당일·목록, 서버 오픈 멘션 역할, 환율 기록, 시간·UTC·환율 음성 채널 설정을 연결했습니다. 큐브세일의 예약 전용 설정은 웹 목록에서 제외했습니다.
- 설정 변경 확인은 페이지 안에서 채널·ON/OFF·역할·즉시 전송/이름 변경/당일 메시지 삭제 동작을 표시합니다. 저장 중 입력을 잠그며 음성 채널에는 해당 설정만 표시합니다.
- 웹과 Discord 명령어의 처리를 통합했습니다. 이전 역할·설정 충돌, 음성 채널 중복 종류, 다른 서버의 채널·역할과 권한을 검사합니다.
- 설정을 먼저 저장한 뒤 즉시 동작을 실행합니다. 저장 실패 시 복원하고, 즉시 동작 또는 후속 기록 실패는 저장 완료와 구분해 경고합니다. 중복 저장으로 즉시 메시지가 반복되지 않도록 했습니다.

### 추가됨 — 홈페이지 알림 설정 저장 (운영 배포 전)
- 실제 서버·텍스트 채널에서 공지·미라클 타임·캐시이동·우르스 알림 ON/OFF를 각각 저장하는 화면과 API를 추가했습니다. 저장 시 관리자·CSRF·채널 소속·봇 권한을 검사합니다.
- Discord 설정 명령과 같은 저장 처리를 사용하고 다른 알림은 변경하지 않습니다. 동시에 바뀐 설정은 재조회를 안내하고 파일 저장 실패 시 기존 상태를 유지합니다.
- 운영자의 홈페이지 자동 설정 적용과 실제 test 채널 조회를 확인했습니다. 이번 저장 기능은 로컬 검증 단계이며 운영 채널은 변경하지 않았습니다.

### 추가됨 — 홈페이지 최초 설정 도구
- 서버에서 Application ID·Client Secret을 직접 입력하고 root 전용 파일에 저장하는 도구를 준비했습니다. Secret은 숨김 입력과 재입력 확인을 사용합니다.
- 사용자가 마지막 적용 질문에 동의하면 서비스의 자동 읽기 연결과 재시작을 수행합니다. 기존 `.env`·홈페이지 설정을 읽거나 덮어쓰지 않습니다. 아직 실제 비밀값 입력·서비스 설정 적용은 하지 않았습니다.

### 배포 확인 — 홈페이지 코드와 봇 조회 연결 준비
- 기능 커밋 `1f08912`를 GitHub와 메인 서버에 반영했습니다. 로컬 기존 456개·홈페이지 18개, 서버 홈페이지 18개 테스트가 통과했습니다.
- 2026-09-15 09:56:03 UTC 메인 봇 재시작 후 active·Discord 재접속·시작 이후 오류 0건과 배포 파일 13개 일치를 확인했습니다.
- 홈페이지 코드는 배포됐지만 서버의 8766 조회 서버는 비활성 상태입니다. 서버 측 OAuth 설정·활성화와 실제 채널 조회 검증은 남아 있으며 운영 설정·DB는 변경하지 않았습니다.

### 추가됨 — 홈페이지 실제 서버·채널 설정 조회 준비 (운영 미적용)
- 로그인 후 서버를 선택하는 읽기 전용 화면과 채널별 활성 기능·멘션 역할·봇 권한 조회를 추가했습니다. 조회마다 Discord 관리자 권한을 다시 확인합니다.
- 명시적으로 활성화한 경우에만 봇과 같은 프로세스에서 현재 설정을 제공합니다. 운영 설정 파일을 직접 수정하지 않으며 홈페이지 저장 API는 없습니다.
- 홈페이지·기존 알림 설정 관련 25개 검증을 통과했습니다. 운영 서버 배포·실제 채널 조회 검증은 아직 진행하지 않았습니다.

### 추가됨 — 홈페이지 Discord 로그인 로컬 연결 준비
- Discord OAuth2 로그인·관리자 서버 목록 조회·로그아웃 API와 연결 안내 화면을 추가했습니다. 기존 봇과 같은 관리자 기준을 사용하며 실제 설정 변경은 제공하지 않습니다.
- 브라우저에 토큰을 반환하지 않고 서버 메모리에만 보관합니다. 로그인 요청 검증·세션 만료·로그아웃 CSRF 검사·공개 파일 제한을 추가했습니다.
- 운영자 직접 입력 전에는 로그인 준비 안내를 표시합니다. 모의 API 검증을 완료했으며 실제 Discord 로그인·운영 배포는 아직 확인하지 않았습니다.

### 추가됨 — 샤벳 홈페이지 로컬 시안
- 소개·대시보드·명령어 검색과 분류 화면을 추가했습니다. 명령어 사용 이미지는 촬영본을 넣을 자리로 표시합니다.
- 가상 서버·채널에서 알림·역할·시간 표시를 선택하고 브라우저에만 저장할 수 있습니다. 권한 부족 예시와 미저장 변경사항 보호를 제공합니다.
- Discord 로그인·운영 설정·공개 배포는 연결하지 않았습니다. 실행 방법은 `website/README.md`에 기록했습니다.

### 배포 확인 — 도움말·채널설정 통합
- 기능 커밋 `6e72284`를 GitHub와 메인 서버에 반영했습니다. 로컬 전체 456개·서버 관련 84개 테스트를 통과했습니다.
- 2026-09-15 08:46:27 UTC 재시작 후 Discord 재연결, active 상태, 시작 이후 오류 0건과 배포 파일7개 해시 일치를 확인했습니다.
- Discord 전역 명령에서 `/채널설정`의 한국어 이름·관리자 권한, 도움말 등록과 기존 개별 설정 명령 제외를 확인했습니다. 도움말과 설정 응답은 실행한 본인에게만 표시합니다.

### 변경됨 — 채널 설정 명령 통합 (서버 배포 전)
- 전체 456개 테스트를 통과했고 승인된 test 채널에 사용법·가상 설정 결과·조회 예시를 게시했습니다.
- 공지·이벤트·서버 오픈·시간·환율 설정을 `/채널설정`으로 통합했습니다. 옵션 없이 현재 서버 설정을 조회하고 종류·채널·동작으로 선택한 항목만 변경합니다.
- 기존 개별 설정 명령은 전역 등록에서 제외하고 관리자 도움말을 변경했습니다. 저장 데이터와 서버 오픈 멘션 역할, 기존 권한 검사·전송 동작은 유지합니다.
- 불완전한 입력·잘못된 채널 종류·무관한 역할 입력은 저장 전에 안내합니다. 큐브세일은 채널 예약만 지원한다고 표시합니다.

## 2026-09-13

### 변경됨 — 분류를 선택하는 도움말 (서버 배포 전)
- `/명령어` 첫 화면을 짧은 기능 소개로 바꾸고 공지·이벤트, 랭킹·아이템, 계산기, 시뮬레이터, 편의 메뉴를 추가했습니다. 선택한 분류의 기존 명령어와 사용 예시를 같은 메시지에 표시합니다.
- 처음 화면으로 돌아가기, 현재 선택 표시, 실행자 제한과 비공개 응답을 유지하며 15분 뒤 다시 여는 방법을 안내합니다. 분류 이동·전체 명령어 포함·다른 사용자 제한·홈 복귀 검증을 통과했습니다.

### 배포 확인 — 응답 양식 정리
- 기능 커밋 `623b715`를 GitHub와 메인 봇에 반영했습니다. 로컬 전체 453개·서버 관련 77개 테스트를 통과했습니다.
- 2026-09-14 01:34:40 UTC 재시작 후 Discord Gateway 재연결, 서비스 active, 시작 이후 오류 0건과 배포 파일 17개의 해시 일치를 확인했습니다.

### 변경됨 — 응답 양식 최종 정리
- 계산기·공지·일정 응답에 검토한 간결형 양식을 적용하고 기존 전용 이모지를 유지합니다. 스스비·시그니처·원더베리는 기존 양식으로 확정했습니다.
- 캐시이동의 오른쪽 블레어 썸네일·별도 남은 시간, 큐브세일 제목 크기와 제목 공백을 정리했습니다. 향후 기준은 `docs/UI_STYLE.md`에 기록했습니다.
- 전체 453개 테스트를 통과했으며 test 채널의 예시를 실제 Discord 메시지로 검토했습니다.

### 수정됨 — 캐시이동 남은 시간 안내 (서버 배포 전)
- 기간 아래 별도 한 줄에 시작 전에는 시작까지, 진행 중에는 종료까지 남은 시간을 Discord 상대 시간으로 표시합니다.

### 수정됨 — 캐시이동 모바일 가독성 (서버 배포 전)
- 캐시이동 제목을 작은 별로 바꾸고 기간의 요일·상대 시간을 생략했습니다. 참여 조건과 이동 설명을 줄여 오른쪽 블레어 썸네일 옆에서 과도하게 줄이 꺾이지 않도록 정리했습니다.

### 수정됨 — 제목 간격·이벤트 이미지 배치 (서버 배포 전)
- 제목 이모지 뒤의 넓은 공백을 일반 공백 한 칸으로 맞췄습니다. 캐시이동은 ↔️ 제목과 오른쪽 블레어 썸네일로 변경했습니다.
- 큐브세일은 작은 작성자 영역 대신 큐브 이모지를 포함한 일반 제목으로 표시합니다. 관련 테스트 24개를 통과했습니다.

### 수정됨 — 캐시 시뮬레이터 기존 양식 복원 (서버 배포 전)
- 사용자 검토에 따라 스스비·시그니처·원더베리의 기존 제목·색상·하단 누적 횟수/비용과 텍스트 기대값 안내를 복원했습니다. 향후 양식 통일에서도 제외하도록 기준 문서에 기록했습니다.
- 추첨·비용 계산과 결과 이미지, 영구 펫의 보라색 슬롯은 유지합니다.

### 수정됨 — 전용 이모지 유지 및 추가 양식 검증 (서버 배포 전)
- HEXA·비약·에픽 던전·EXP 교환권·스스비의 기존 제목 이모지와 재료 이모지를 복원했습니다. 기존 전용 이모지가 있으면 별을 덧붙이지 않습니다.
- 에무새의 새장 `test`에 계산기·시뮬레이터·보스·도움말·이벤트·패치 수정 등 양식 예시 12개를 게시했습니다. 가상 일정과 고정 추첨 결과, 과거 공지는 예시에 표시했습니다.
- 전체 453개 테스트가 통과했습니다. 예시는 화면 검토용이며 운영 코드 배포 및 버튼 동작 검증은 포함하지 않았습니다.

### 추가됨 — 응답 양식 기준 문서
- `docs/UI_STYLE.md`에 간결형 양식, 전용 이모지 우선 규칙, 계산·시뮬레이터·공지·일정 예시와 확인 절차를 기록했습니다. `PROJECT_RULES.md`에서 응답 작업 전 필수 참조 문서로 연결했습니다.
- 모든 제목을 별로 교체하던 초기 시안에서 기존 전용 이모지는 유지하는 방향으로 기준을 정정했습니다. 해당 이모지 복원은 아직 코드·test 예시에 반영 전이며 운영 배포도 하지 않았습니다.

### 변경됨 — 명령어 응답 간결형 통일 (서버 배포 전)
- 작은 전용 별과 한국어 제목을 공통 적용하고 계산기 결과의 장식·숫자 강조·문단 간격을 통일했습니다. 긴 공식 제목은 별을 포함해 Discord 제목 제한 안에 표시합니다.
- 스스비·시그니처·원더베리의 누적 사용량·비용을 본문 항목으로 옮기고 기대값 버튼도 간결한 임베드로 표시합니다. 기존 이미지와 재추첨 동작은 유지합니다.
- 일정은 항목명 아래 시작·종료를 구분하고, 패치·알려진 이슈·자동 공지에도 같은 제목 규칙을 적용합니다. 랭킹·퍼밀리어·연마석 이미지 위 제목과 설정 성공 안내도 맞췄습니다.
- 전체 452개 테스트를 통과했으며 실제 계산 함수가 생성한 예시 7개의 임베드 길이 제한을 확인했습니다.

### 추가됨 — 전용 별 이모지 시안
- 사용자 피드백에 따라 별의 내부 크기를 25% 줄인 버전을 등록하고, 기존 예시 4개와 같은 내용의 간결형·결과 강조형·항목 구분형 비교 예시 3개에 적용했습니다.
- 금색 두 면으로 구성한 전용 별 이모지 `ppojji_star`를 봇 애플리케이션에 등록하고, 에무새의 새장 `test` 채널의 기존 비교 예시 4개 제목에 적용했습니다. Discord 화면에서 이모지 렌더링을 확인했습니다.
- 투명 PNG 원본은 `assets/emoji/ppojji-star-128.png`와 `ppojji-star-256.png`로 보관합니다. 전체 명령어 양식은 검토 중이며 자동 응답에는 아직 적용하지 않았습니다.

### 배포 확인 — QA 보완 사항
- 기능 커밋 `f474c67`을 GitHub와 메인 서버에 반영했습니다. 로컬 전체 451개·서버 관련 48개 테스트를 통과했고, 2026-09-13 23:26:32 UTC 재시작 후 Discord 재연결·오류 0건을 확인했습니다.
- `에무새의 새장 / #test`에서 `/캐샵일정`을 다시 실행해 판매 종류 표시와 하단 꼬리말 제거를 확인했습니다.

### 수정됨 — 시그니처 기대 비용
- 평균 횟수를 정수로 반올림한 뒤 가격을 곱하던 시그니처 기대 비용을 `7,900 NX ÷ 획득 확률`로 계산하고 마지막에 NX만 반올림하도록 수정했습니다. 7%는 112,857 NX, 30%는 26,333 NX로 안내합니다.
- 실제 뽑기와 누적 구매 비용 계산은 유지합니다.

### 수정됨 — 캐시샵 일정 식별 정보
- 날짜만 나열되던 `/캐샵일정`에 원본 클라이언트 상품 기준 판매 종류를 추가했습니다. 상세 상품 목록은 생략하고 종류와 기간을 함께 표시합니다.
- 긴 목록은 Discord 필드 길이 제한에 맞춰 나누며 파일명·추출 정보 등을 표시하던 하단 꼬리말은 제거합니다.

### 수정됨 — 패치·알려진 이슈 감시 오류 복구
- GPT API 오류가 발생하면 패치·알려진 이슈 수정 감시 작업이 종료될 수 있던 문제를 수정했습니다. 오류 종류를 기록하고 다음 5분 주기에 저장된 미전송 변경분을 재시도합니다.
- 실제 반복 작업에 GPT 오류를 주입한 뒤 다음 회차까지 실행되는 회귀 테스트를 추가했습니다.

### 변경됨 — 랭킹 DB 주간 이중 백업
- import-only 메인 봇에서는 실행되지 않던 수집 완료 후 일일 백업 분기를 제거하고, 매주 일요일 03:30 UTC 이후 별도 systemd timer가 운영 `ranking.db`를 백업하도록 변경했습니다.
- SQLite 온라인 백업과 전체 무결성 검사 후 gzip 압축·SHA-256 체크섬을 만들고, 메인 서버와 보조 1번 서버에 각각 최근 4개를 순환 보관합니다. 원격 전송이나 해시 검증이 실패하면 새 로컬 백업을 보존하고 기존 파일을 정리하지 않습니다.
- DB 구조 변경·대규모 데이터 수정 전 수동 백업은 유지하며 일반 코드 배포에는 전체 DB 백업을 반복하지 않습니다.
- 기능 커밋 `25b2cf1`을 GitHub와 메인 서버에 반영했습니다. 작은 DB로 압축 복원·순환 정리·메인→보조 전송을 검증한 뒤 운영 3.26GB DB를 실제 백업했습니다. 전체 무결성 검사와 양쪽 SHA-256이 일치했고 1.32GB로 압축됐으며 서비스는 성공 종료했습니다. 다음 자동 실행은 2026-09-20 03:37:38 UTC로 예약됐습니다.

### 추가됨 — 모든 슬래시 명령어 영문 이름
- 일반·소유자·관리자용 전역 슬래시 명령어 50개에 영문 기본 이름과 기존 한국어 현지화 이름을 함께 등록했습니다. 한국어 Discord에서는 기존 명령어 이름을 유지하고 `/ranking`, `/epic-dungeon`, `/cashshop`, `/news-alert` 같은 영문 이름으로도 검색할 수 있습니다.
- 별도 명령어를 두 벌 만들지 않고 `COMMAND_NAME_LOCALIZATIONS` 한 곳에서 대응 이름을 관리해 Discord의 전역 명령어 개수와 기존 콜백·권한·설정을 그대로 유지합니다.
- 기능 커밋 `c43e8f7`을 GitHub와 메인 서버에 반영했습니다. 로컬 전체 테스트 446개와 서버 관련 테스트 11개가 통과했으며, 등록된 50개 명령어의 영문·한국어 이름 대응을 전부 확인했습니다. 봇은 2026-09-13 19:41:39 UTC 재시작 후 `active`, Discord 연결 정상, 자동 재시작 0회, 시작 후 오류 0건입니다.

### 추가됨 — `/랭킹` 개인 최근 캐릭터 자동완성
- `/랭킹`의 닉네임 입력창에 해당 Discord 사용자가 성공적으로 조회한 최근 캐릭터를 최신 순으로 최대 10개 표시합니다. 입력한 글자가 있으면 개인 목록 안에서 이름을 검색합니다.
- 다른 사용자의 조회 목록과 존재하지 않거나 조회에 실패한 이름은 저장하지 않습니다. 같은 캐릭터를 다시 조회하면 중복 없이 맨 위로 이동하며, 기존 마지막 캐릭터 기본값은 첫 최근 목록으로 자동 이전됩니다.
- 기능 커밋 `3ba1a34`를 GitHub와 메인 서버에 반영했습니다. 로컬 전체 테스트 446개와 서버 관련 테스트 5개가 통과했으며, 배포 전 3.2GB 랭킹 DB 백업본의 전체 무결성 검사가 `ok`임을 확인했습니다. 운영 DB의 기존 기본 캐릭터 17건은 최근 목록으로 모두 이전되어 누락이 0건입니다. 봇은 2026-09-13 19:23:51 UTC 재시작 후 `active`, Discord 연결 정상, 자동 재시작 0회, 시작 후 오류 0건입니다.

## 2026-09-12

### 변경됨 — `/알려진이슈` 현재 미해결 목록 표시
- 명령어의 한국어 이름을 `/알려진이슈`로 맞추고, 링크만 표시하던 결과에 공식 문서의 `Current Known Issues` 구역을 GPT로 한국어 요약해 함께 표시합니다. `Resolved Issues`는 명령 결과에서 제외합니다.
- 같은 문서 본문은 메모리의 기존 요약을 재사용하고, 문서 ID나 현재 이슈 본문이 바뀐 경우에만 GPT 요약을 다시 생성합니다.
- 커밋 `d26dc46`을 GitHub와 메인 서버에 반영했습니다. 로컬 전체 테스트 443개와 서버 관련 테스트 6개가 통과했으며 실제 v.271 문서에서 현재 이슈는 포함되고 해결된 Glowing Cubes 항목은 제외됨을 확인했습니다. 봇은 2026-09-12 20:46:55 UTC 재시작 후 `active`, Discord 연결 정상, 자동 재시작 0회, 시작 후 오류 0건입니다.

### 추가됨 — Known Issues 수정 감지와 조회 명령어
- 공식 Nexon 고객지원 `Service Issues` API에서 최신 GMS Known Issues 문서를 찾아 5분마다 본문을 비교합니다. 최초 실행에는 기준점만 저장하고 이후 추가·수정·삭제 및 해결 목록 이동을 GPT가 한국어로 정리해 기존 공지 채널에 노란색 `MapleStory | KNOWN ISSUES UPDATE` 임베드로 전송합니다.
- 패치 수정 이력과 분리된 `known-issues-history.db`에 본문 기준점·변경분·요약·채널별 성공 기록을 저장해 재시작 후 중복 전송을 막고 실패한 채널만 재시도합니다.
- `/알려진이슈` 명령어에서 현재 버전의 공식 Known Issues 제목과 링크를 확인할 수 있으며 개인 설치·서버·DM을 지원합니다.
- 기능 커밋 `ff22c02`를 GitHub와 메인 서버에 반영했습니다. 로컬 전체 테스트 441개와 서버 관련 테스트 8개가 통과했으며, 운영 API에서 v.271 문서를 확인했습니다. 봇은 2026-09-12 20:23:56 UTC 재시작 후 `active`, Discord 연결 정상, 자동 재시작 0회, 시작 후 오류 0건입니다. 최초 기준점 1개와 수정 이력 0건을 확인해 과거 내용이 공지되지 않았습니다.

### 추가됨 — 캐시샵 클라이언트 예약 판매 일정
- `/캐샵일정`에서 v.271 클라이언트 `Etc/Commodity.img`에 저장된 예약 판매 기간을 진행 중·예정으로 나눠 Discord 현지 시각으로 표시합니다. 판매 상품 목록은 표시하지 않고, 끝난 기간은 자동으로 제외하며 공식 판매 확정 전 정보라는 안내를 함께 표시합니다.
- `/캐샵`에서는 외부 캐시샵 데이터 마이닝 사이트 링크를 제거해 최신 공식 `Cash Shop Update` 공지만 안내합니다.
- 커밋 `2753890`을 GitHub와 메인 서버에 반영했습니다. 로컬 전체 테스트 436개와 서버 관련 테스트 5개가 통과했으며, 봇은 2026-09-12 20:08:03 UTC 재시작 후 `active`, Discord 연결 정상, 자동 재시작 0회, 시작 후 오류 0건입니다.

### 변경됨 — 원더베리 영구 펫 슬롯 색상
- `/원더베리` 결과가 `Permanent`인 펫은 확률과 관계없이 보라색 스페셜 슬롯에 표시합니다. 기존 프리렌 0.25% 희귀 펫의 보라색 슬롯도 유지합니다.
- 커밋 `f7e3d0b`를 푸시하고 메인 서버 테스트 10개 통과 후 봇을 재시작했습니다. 2026-09-12 19:49:06 UTC Discord 연결 정상, 자동 재시작 0회, 시작 후 Traceback·오류 0건입니다.

### 운영 배포 완료 — 프리렌 캐시 시뮬레이터·랭킹 수집 개선
- 커밋 `d5934ff`를 GitHub에 푸시하고 메인 봇과 랭킹 수집기 4대에 적용했습니다. 메인에는 프리렌 시뮬레이터 코드·아이콘·정적 화면 자산을, 모든 수집기에는 랭킹 코드와 현재 런타임 의존 파일을 해시 검증·백업 후 교체했습니다.
- 재시작 전 보조 3대에서 오래된 번역 모듈과 새 봇 코드의 import 불일치를 발견해 현재 커밋의 의존 파일로 맞췄습니다. 보조 서버별 랭킹 테스트 43개와 메인의 시뮬레이터·랭킹 테스트 41개가 통과한 뒤에만 서비스를 재시작했습니다.
- 메인 봇은 2026-09-12 19:12:08 UTC Discord 연결 정상, 메인·보조 랭킹 수집기 4대는 같은 시각 새 작업을 시작했습니다. 다섯 서비스 모두 `active`, 자동 재시작 0회, 시작 후 Traceback·오류 0건입니다.

### 추가됨 — 프리렌 시그니처·원더베리 정적 시뮬레이터
- `/시그니처`와 `/원더베리`에서 1회 또는 5회 결과를 정적 PNG 한 장으로 표시합니다. 시그니처는 기존 스스비 화면을, 원더베리는 v.271 GMS 클라이언트에서 추출한 새 배경·일반/스페셜 슬롯과 실제 펫 아이콘을 사용하며 GIF·영상은 전송하지 않습니다.
- 명령 실행과 다시 뽑기 때 공식 확률표를 새로 읽습니다. 시그니처 비용은 낱개 7,900 NX·10개 묶음 79,000 NX, 원더베리는 낱개 4,000 NX·11개 묶음 40,000 NX를 기준으로 최저 비용을 계산합니다. 두 명령 모두 누적 횟수·비용과 결과별 기대값을 제공합니다.
- 공식 확률표 마지막의 이름 없는 100% 합계 행을 보상으로 읽지 않도록 제외해 실제 추첨 확률 합계가 약 100%가 되게 했습니다.
- 설치된 클라이언트에서 관련 아이콘·원더베리 정적 화면을 다시 추출하는 도구와 실행에 필요한 데이터·자산을 추가했습니다. OpenCV는 자산을 갱신할 때만 사용하며 운영 봇 실행 의존성에는 넣지 않았습니다.

### 수정됨 — 랭킹 수집 검사 오탐 및 대표 랭킹 반복 수집
- 전역 목록인 업적 랭킹을 네 월드별로 중복 요청하지 않고 네 샤드가 한 번의 전체 수집을 나눠 처리하도록 변경했습니다. 감사 검사도 업적 완료 표식을 전역 수집 기준으로 확인한 뒤 당일 경험치 기록의 실제 월드별 인원으로 나눕니다. 서버별 월드 설정 순서가 달라도 업적 저장·검사 키는 `45`로 고정합니다.
- 당일 완료된 유니온·업적 수집 상태를 DB에서 확인해 작업이나 서비스가 재시작되어도 같은 랭킹을 다시 훑지 않습니다.
- 유니온·업적 값이 대기 테이블에 안전하게 보관된 경우는 진단 로그에 남기되 관리자 오류 DM에서는 제외합니다. 실제 완료 표식·페이지·DB 자료 누락은 계속 알립니다.

### 운영 배포 완료 — 패치 추가 수정 알림 가독성 개선
- 커밋 `e248225`를 푸시하고 메인 서버의 `maple_bot.py`와 `patch_ai.py`를 해시 확인·백업 후 교체했습니다. 로컬 전체 테스트 421개가 통과했습니다.
- 운영 파일 해시가 커밋과 일치하며 봇은 2026-09-12 10:42:59 UTC 재시작 후 `active`, Discord 연결 정상, 최근 오류 0건입니다.

### 수정됨 — 변경 전·후 비교 간격 조정
- 기존 불꽃·스타캐치 시안과 향후 자동 알림에서 `변경 전` 내용 뒤에만 빈 줄 하나를 추가해 두 비교 결과가 붙어 보이지 않게 했습니다. 그 밖의 촘촘한 문단 간격은 유지했습니다.

### 수정됨 — 추가 수정 알림의 과한 여백 축소
- 항목 제목은 설명에 붙이고 설명과 비교 사이·번호 항목 사이·원문 링크 앞에만 빈 줄을 남겼습니다. `변경 전:`과 `변경 후:` 묶음 사이는 붙여 세로 길이를 줄였으며 자동 생성 규칙과 기존 불꽃·스타캐치 임베드 시안을 같은 형태로 맞췄습니다.

### 수정됨 — 불꽃·스타캐치 공지 비교 제목 줄바꿈
- 사용자가 지정한 기존 불꽃·스타캐치 임베드도 색상 원형 표시를 없애고 `변경 전:`·`변경 후:`·`추가:` 다음 줄에 내용을 배치했습니다. 번호와 문단 간격을 유지하고 원본 보관 후 재조회로 확인했습니다.

### 수정됨 — 스타포스 마도서 공지 비교 형식 시안
- 기존 마도서 임베드 한 건을 색상 원형 표시 없이 `변경 전:`·`변경 후:` 다음 줄에 내용을 배치하는 형식으로 편집했습니다. 번호·문단 여백과 노란 테두리를 유지하고 원본 보관 및 재조회 검증을 완료했습니다.

### 변경됨 — 자동 추가 수정 알림의 번호·문단 간격
- 확인한 임베드 시안에 맞춰 GPT 요약에 ①·② 번호, ⚪ 이전·🔴 변경·🟢 추가 표시와 문단 간격을 지정했습니다. 제목·설명·비교 묶음은 빈 줄로 구분하고 이전/변경 두 줄과 관련 설명은 붙입니다.
- 원문 링크를 본문 마지막에 빈 줄 하나로 구분해 표시합니다.

### 수정됨 — 불꽃·스타캐치 공지 여백 시안
- 최종 간격 조정 시안은 제목·설명·이전/변경 묶음 사이와 원문 링크 앞에 빈 줄을 넣었습니다. 이전/변경 두 줄은 붙이고 스타포스 두 문장은 줄바꿈으로 구분했으며 기존 메시지 편집 후 재조회로 확인했습니다.
- 후속 피드백에 따라 항목 내부의 빈 줄을 없애고 스타포스 설명을 한 문단으로 합쳤습니다. ①과 ② 사이에만 빈 줄을 유지했습니다.
- 기존 공지 한 건을 ①·② 번호와 문단 간 빈 줄로 정리하고, ⚪ 이전·🔴 변경·🟢 추가 표시를 적용했습니다. 항목 사이 여백을 넓혔으며 원본 보관 후 Discord 메시지를 재조회해 확인했습니다.

### 변경됨 — 향후 패치 추가 수정 알림 구성
- 수정 감지 당시 상위 제목과 주변 표·문장을 함께 보관하고 GPT에 전달해 어떤 이벤트·미션·아이템의 변경인지 먼저 설명하도록 했습니다.
- 노란색 `MapleStory | PATCH UPDATE` 임베드에 버전 제목, 대상·맥락, 변경 전·후 또는 추가·삭제된 내용, 원문 링크를 표시합니다. 중복 `핵심` 항목과 별도 `패치` 필드는 제외합니다.
- 기존 전송 기록과 재시도 동작을 유지하며, 기존 DB에 맥락 저장 열을 추가합니다.

### 수정됨 — 패치 추가 수정 임베드 구성 시안
- 사용자 피드백에 따라 시안 4건의 중복 `핵심` 항목을 제거하고 대상·맥락, 변경 내용, 원문 링크로 간소화했습니다.
- 추가 요청에 따라 탈라하트 심볼·야쿠자 보스·불꽃/스타캐치 공지 3건도 같은 구성으로 편집했습니다. 대상 맥락과 추가된 안내를 구분하고 불꽃의 확률 기준 안내 정정임을 명시했으며 원본 보관·재조회 검증을 완료했습니다.
- 사용자 요청으로 스타포스 마도서 관련 기존 추가 수정 알림 1건을 편집했습니다. `MapleStory | PATCH UPDATE`와 버전 제목, 프리렌 이벤트·미션 맥락, 변경 전·후, 변경 핵심을 나눠 표시하고 원본을 보관한 뒤 재조회로 확인했습니다.
- 이번 작업은 기존 메시지의 디자인 확인용 편집이며 자동 요약·임베드 생성 코드는 아직 변경하지 않았습니다.

### 수정됨 — 저장된 썬데이 샤이닝 표시
- 사용자 요청에 따라 v.271 저장 일정의 UTC 9/13·9/27·10/11·10/25·11/8에 기존 반짝이 이모지와 `스페셜: 샤이닝 스타포스` 표시를 추가했습니다. 두 스타포스 혜택과 다른 일정 내용·전송 기록은 유지했습니다.
- 기존 썬데이 목록 메시지 1개를 편집하고 저장값·메시지를 재조회했습니다. 원본을 보관했으며 봇은 2026-09-12 09:33:16 UTC 재시작 후 정상 실행 상태입니다.

## 2026-09-11

### 운영 배포 완료 — 공식 Discord 수집·서버 오픈 알림
- 코드 커밋 `31ed2b8`을 푸시하고 메인 15개 파일과 보조 1번의 별도 수집 코드를 배포했습니다. 메인 기존 파일 해시를 확인하고 `/home/ubuntu/maple-release-31ed2b8/before/`에 백업했습니다.
- 로컬·메인 전체 테스트 420개, 보조 관련 테스트 13개 통과. 실제 공지 10개 수집·SSH 전송·기준점 저장 후 기존 글 전송 0건을 확인하고 자동 처리를 활성화했습니다. 메인 봇은 2026-09-11 09:51:45 UTC 재시작 후 연결 정상·오류 0건이며 보조 수집기 자동 재시작도 0회입니다.
- 실제 새 서버 오픈 공지는 아직 발생하지 않았습니다. 오픈 멘션은 모의 채널에서 중복·재시작 방지를 검증했으며 운영에서 시험 멘션은 보내지 않았습니다. 앞선 운영 미적용 표기는 당시 진행 기록입니다.

### 수정됨 — 공식 공지 기준 서버 오픈 멘션
- 서버 상태 API가 일시적으로 정상 응답하거나 점검 종료 예정 시각이 지났다는 이유로 오픈 멘션을 보내던 경로를 중단했습니다. 공식 announcements의 명확한 `Game is up!` 안내에만 기존 오픈 메시지와 채널별 역할 멘션을 전송합니다.
- 점검·원문·채널별 전송 전 기록으로 공지 수정·중복 공지·재시작에 따른 멘션 재전송을 막습니다. 인용·취소선·질문·예정 문구와 캐시샵·채널 점검 완료는 오픈으로 취급하지 않습니다. 전송 결과가 불명확하면 자동 재전송 대신 관리자 확인을 요청합니다.
- 보조 1번에서 브라우저 재실행 후 로그인 유지와 실제 최신 공지 10개 추출을 확인했습니다. 이번 배포는 앞서 준비한 공식 Discord 수집·번역·색상 변경을 포함합니다.

### 변경됨 — 공식 Discord 공지 색상
- 일반 공지는 노랑, 점검·기능 제한은 주황, 완료·정상화는 초록으로 표시하도록 로컬 코드에 반영했습니다. 취소선·인사말을 제외한 첫 안내 문단을 기준으로 판별하며 관련 테스트 8개가 통과했습니다. 자동 게시 코드의 운영 배포는 아직 전입니다.
- 기존 관리자 DM의 점검 완료 공지를 초록색으로 변경하고 재조회로 확인했습니다.

### 검증 — 공식 Discord 공지 관리자 DM
- 사용자 요청으로 테스트 DM의 `사용자 제공 원문 · GPT 상세 번역/DM 전송 테스트` 하단 문구를 제거하고 재조회로 확인했습니다.
- 서버의 로그인된 브라우저에서 공식 공지 10개를 읽는 것을 확인했습니다. 별도로 사용자가 제공한 최신 점검 완료 원문을 GPT 상세 번역하고 관리자 DM에 테스트 1건을 전송한 뒤 본문을 재조회해 확인했습니다.
- 보상 대상·수령 기간 3주·교환 불가·유효 기간 30일을 번역에서 확인했습니다. 이번 DM은 사용자 제공 원문으로 수행한 테스트이며 실시간 수집부터 게시까지의 통합 검증은 아직 완료하지 않았습니다.

### 운영 준비 — 서버 로그인 화면
- 보조 1번에 Chromium·Playwright·noVNC 로그인 도구를 설치했습니다. 사용자 승인 후 설치된 Chromium 실행 파일에만 사용자 네임스페이스를 허용하는 AppArmor 규칙을 적용해 샌드박스를 유지한 브라우저 실행을 확인했습니다.
- SSH 터널을 통한 로그인 화면 연결을 확인했습니다. 사용자 직접 로그인 대기 상태이며 실제 공지 수집·자동 게시 서비스는 아직 활성화하지 않았습니다. 기존 랭킹 수집 서비스는 정상 실행 중입니다.

### 추가됨 — 공식 Discord 수집 검증 모드
- 보조 서버의 전용 Chromium 화면에서 공식 announcements를 수집하는 도구와 메인 봇의 선택적 처리 루프를 추가했습니다. 최초 기준점·새 글·수정 기록과 채널별 전송 결과를 별도 DB에 보관하며 기존 랭킹 전송 함수를 목적지별로 재사용합니다.
- 알려진 홈페이지 링크의 명확한 단순 소개를 제외하고 GPT 상세 번역, 10초 초과 원문 우선 게시·후속 편집, 이미지 원문 전달, 수집 상태 알림을 구현했습니다. 검증 모드에서는 번역·게시하지 않습니다.
- 설치·로그인·서비스 예시와 운영 한계를 문서화했습니다. 서버 설치·로그인·실제 수집 및 자동 게시 검증 전이며 운영에는 적용하지 않았습니다.

## 2026-09-10

### 수정됨 — 불꽃 확률 표현 명확화
- 사용자 확인에 따라 기존 패치 추가 수정 임베드 2개의 첫 문장을 `추가옵션 재설정 시 옵션 등장 확률`로 교정하고 재조회로 확인했습니다. 검은 환생의 불꽃은 기존 안내, 강력한 환생의 불꽃은 수정된 안내임을 명시했습니다.
- 용어사전의 `reset rate(s)`를 `옵션 등장 확률`로 조정했습니다. 용어사전 변경은 로컬 반영이며 운영 배포 전입니다.

### 운영 배포 완료 — 한국어 요약·랭킹 검사
- 커밋 `c500e08`을 푸시하고 메인 11개 파일, 보조 3대의 랭킹 검사 모듈·테스트 각 2개 파일을 동기화했습니다. 원본은 각 서버의 `/home/ubuntu/maple-release-c500e08/before/`에 보관했습니다.
- 로컬·메인 전체 테스트 407개, 보조별 관련 테스트 29개가 통과했습니다. 배포 파일 해시를 확인했으며 메인 봇은 2026-09-10 19:26:39 UTC 재시작 후 Discord 연결 정상, 오류 0건·자동 재시도 0회입니다. 메인 랭킹 수집기는 실행 상태를 유지했습니다.
- 아래 로컬 반영·배포 전 표기는 당시 기록이며 GPT 한국어 요약, 번역 용어 추가, 중복·대기·실제 누락 구분은 이번 배포로 운영에 적용했습니다.

### 수정됨 — 기존 패치 알림 임베드
- 사용자 요청으로 v.271의 9월 10일 추가 수정 알림을 공지 채널 2곳에서 각 1개씩 편집했습니다. `Burning Rebirth Flame` 이름을 유지하고 `재설정 확률`, `스타캐치`, 성공률 증가 효과 상시 적용으로 설명을 교정했습니다. 원본 임베드를 보관하고 편집 후 메시지를 재조회해 확인했습니다. 코드 배포·서비스 재시작은 수행하지 않았습니다.

### 변경됨 — GPT 한국어 요약 통합
- 일반 공지와 패치 추가 수정은 `gpt-5.6-luna`가 한국어 요약을 한 번에 생성하고, 별도 Google 번역을 호출하지 않습니다. 관련 용어사전과 관리자 교정값(DM 우선)을 GPT에 전달하며 기존 요약 재사용·채널별 전송 이력을 유지합니다.
- 썬데이·캐시샵의 별도 Google 번역은 유지합니다. 별도 패치 요약 테스트 도구는 OpenAI 키만 사용합니다. 로컬 반영이며 운영 배포 전입니다.

### 수정됨 — 패치 번역 용어
- `reset rate(s)`를 `재설정 확률`, `Star Catching`을 `스타캐치`로 보호하도록 용어사전에 추가했습니다. `Bonus Stats → 추가옵션`과 환생의 불꽃 이름은 기존 등록을 유지합니다. 로컬 반영이며 운영 배포 전입니다.

### 수정됨 — 수집 중복과 실제 누락 구분
- 공식 페이지 간 같은 캐릭터는 한 명으로 집계하고 중복만으로 경고 DM을 보내지 않도록 수정했습니다. 원본 페이지와 중복 횟수 로그는 보존합니다.
- 유니온·업적 값이 정상 대기 테이블에 있는 경우 당일 경험치 기록 대기로 안내하고, 실제 저장 누락과 구분합니다. 전역 업적 응답의 월드 미확인 이름을 네 월드에 반복 집계하지 않습니다. 완료 표식·페이지 누락·인원 감소 경고는 유지합니다. 로컬 반영이며 운영 배포 전입니다.
- 2026-09-09 운영 자료를 읽기 전용 재검사한 결과, 수신된 경험치·유니온·업적의 실제 저장 누락은 0명입니다. 업적 220명은 정상 값이 대기 테이블에 있으며 당일 경험치 수집 페이지에는 없었습니다. 업적 완료 표식 16개 부재는 별도 미완료 경고로 남깁니다. 기존 운영 DB·보고서·DM은 수정하지 않았습니다.

### 운영 배포 완료
- 용어사전 확장·Arcane 표기 정리, `!도핑`·`!항해`·`!패치` 제거, 계산기 제목 간격·적용 여부 표시 개선, `/exp쿠폰` 입력 옵션 제거를 커밋 `32c7655`로 푸시한 뒤 메인 서버에 적용했습니다.
- 로컬·서버 전체 테스트 각각 402개 통과, 배포 파일 10개 해시 일치를 확인했습니다. 메인 봇은 2026-09-10 08:44:46 UTC 재시작 후 Discord에 재연결됐으며 `active`, 재시도 0회, 시작 이후 오류 0건을 확인했습니다. 랭킹 수집기는 `active` 상태를 유지합니다.
- 운영 원본은 `/home/ubuntu/maple-release-32c7655/before/`에 보관했습니다. 아래 로컬 반영·배포 전 기록은 당시 이력이며 이번 배포로 적용 완료했습니다.

### 변경됨
- `/exp쿠폰`의 명령어 입력 옵션 5개를 제거하고 항상 비공개 설정창을 열도록 변경했습니다. 쿠폰·버닝 선택과 시작레벨·경험치·개수 입력은 기존 선택창·수치 입력창에서 처리하며 버닝 기억과 레벨별 쿠폰 제한을 유지합니다. 로컬 반영이며 운영 배포 전입니다.
- 계산기의 적용 여부 선택창에 항목 이름을 함께 표시합니다. 성장의비약은 `하이퍼버닝: 적용/미적용`, `비욘드버닝: 적용/미적용`으로 구분하며 심볼 계산기의 엘라노스에도 같은 표시를 적용합니다. 계산에 전달하는 선택값은 유지합니다. 로컬 반영이며 운영 배포 전입니다.
- 에픽 던전 경험치 계산기 제목의 이모지와 텍스트 사이에 넓은 공백을 적용했습니다. 로컬 반영이며 운영 배포 전입니다.

## 2026-09-09

### 제거됨 — 접두사 명령
- 사용자 요청으로 `!도핑`, `!항해`, `!패치`의 실행 처리·등록·도움말 안내를 제거했습니다. `/도핑`, `/항해`, `/패치`는 유지합니다. 로컬 반영이며 운영 배포 전입니다.

### 추가됨 — 직업·지역명 번역 용어
- 추가 점검으로 심볼·코어·환생의 불꽃·잠재능력·콘텐츠·보스 용어 30개를 보완했습니다. `Arcane Symbol`, `Sol Erda Fragment`, `Experience Nodestone` 등은 짧은 용어보다 우선 적용합니다.
- 사용자 요청에 따라 단독 `Arcane → 아케인`으로 수정하고 장비 문맥 제한을 해제했습니다. `Arcane Umbra → 아케인셰이드`, `Arcane River → 아케인리버`는 긴 용어 우선 처리로 유지합니다.
- 사용자 확인에 따라 오타 `Shagri-La` 항목과 별칭 유지 안내를 제거하고 `Shangri-La → 도원경`만 유지했습니다.
- 사용자 제공 목록에서 `Other`를 제외한 GMS 직업·계열명 62개, 아케인리버·소속 지역명 13개, 그란디스 지역·스토리명 11개를 용어사전에 반영했습니다. 기존 묵현·시아 아스텔·에릴 라이트 및 지역 표기는 유지하고 `Bowmaster` 별칭을 추가했습니다. 직업 `Hero`와 계열 `Heroes`, 지역 `Arcane River`와 단독 `Arcane`, `Arcus`와 `Hotel Arcus`를 구분합니다. 로컬 반영이며 운영 배포 전입니다.

### 배포 절차 정리
- 서버 적용 요청에는 테스트·문서 갱신, 커밋·푸시, 서버 동기화·재시작·정상 상태 확인까지 포함하도록 프로젝트 규칙을 명시했습니다.
- 통합 변경의 코드·테스트·게임 UI 자산·아이템 데이터·문서를 버전 관리 대상으로 정리했습니다. 첨부 원본·미리보기·추출 중간 결과·이전 코드 백업은 로컬에 보존하고 Git에서 제외합니다.

### 운영 통합 배포 완료
- 사용자 요청에 따라 대기 중인 캐시이동 종료 처리, 연마석 기본 1개, 데이터 갱신 DM, 패치 수정 제목 통일, 조사 포함 용어 교정·추가 사전, 업적 검사 수정과 최신 아이템 데이터를 운영에 반영했습니다. 이미 배포된 나머지 미커밋 기능도 로컬·서버 파일과 대조했습니다.
- 메인 서버 전체 테스트 402개와 보조 서버 3대의 랭킹 테스트 각 37개를 통과했습니다. 메인 439개 파일과 보조별 19개 코드·검증 파일의 해시를 확인하고, 다른 파일만 원본 백업 후 교체했습니다. 공지 봇과 수집기 4개를 재시작했으며 2026-09-10 03:13:47 UTC Discord 재연결을 확인했습니다.
- 지정 공지의 남은 용어를 공지 채널 2곳에서 기존 메시지 각 1개씩 교정했습니다. v271 직업별 버튼의 완료 기록을 유지했으며, 데이터 갱신 DM은 최초 기준점 등록만 완료했습니다.
- 운영 설정·DB·로그·체크포인트·기존 전송 및 교정 기록을 배포 파일로 덮어쓰지 않았습니다. 각 서버의 코드 원본은 `/home/ubuntu/maple-integration-20260910T030622Z/before/`에 보관했습니다. 커밋·푸시는 수행하지 않았습니다.

### 미커밋 변경 통합 점검
- 마지막 커밋 `857a69f` 이후 추적 파일 수정과 신규 코드·테스트·자산을 대조했습니다. 아래 과거 항목의 배포 전·후 표기는 당시 기록으로 보존하며, 이번 통합 배포 결과는 별도로 기록합니다.
- 공지·번역: GPT 영어 요약 → Google 한국어 번역, 용어사전 보호·복원과 DM 교정 우선 적용, 요약 불릿·빈 줄 정리, 패치 변경 감시·채널별 재시도, `추가·변경·삭제` 제목 통일, `/패치질문` 등록 제거를 포함합니다. 관련 신규 파일은 `patch_ai.py`, `translation_corrections.py`, `TRANSLATION_GLOSSARY.md`입니다.
- 기존 공지 교정: `news_embed_repair.py`의 지정 공지 용어·조사 교정과 `patch_revision_preview.py`의 v271 직업별 버튼을 포함합니다. 완료 기록을 보존하고, 재시작 시 버튼을 다시 등록합니다.
- 데이터 갱신: `data_update_reminders.py`의 새 패치·신규 스스비 소유자 알림과 최초 실행 기준 등록, 아이템 45,207개·아이콘 15,184개의 최신 검색 데이터를 포함합니다. 추출 보고서·판매 일정·이전 데이터 백업은 로컬 참고 자료로 보존합니다.
- 명령어·화면: `/연마석` 기본 1개·생명 최대 10개·신념 최대 20개, 원본 패널·GIF·0.5초 준비 화면·정적 결과 전환·누계·중복 조작 방지, 계산기 4종과 `/5퍼`의 선택형 화면, 헬럭스 표기·익스트림 검마, `/명령어` 전체 안내와 `/관리자` 분리를 포함합니다. `polisher_ui.py`, `assets/polisher/`, 추출 도구 2개 및 관련 테스트를 함께 관리합니다.
- 이벤트: 썬데이 일정 재조회·기존 전송 기록 보존·샤이닝 표시·메소 표현 정리, 캐시이동 종료 시 비공개 안내·종료 자동 알림·종료 전 첨부파일 닫기 수정을 포함합니다.
- 랭킹: 태그 3개·분야별 색상·긴 이름 폭 조절(`RANKING_TAGS.md`), 마지막 정상 캐릭터 이미지 캐시, 대표 랭킹 기준일별 진행 위치·날짜 경계 보호·경험치 도착 전 대기 저장, 다른 월드 업적 응답의 누락 오인 수정을 포함합니다.
- 개발·보존: 별도 요약 테스트 도구와 새 회귀 테스트, 번역·패치·이미지·교정·알림 상태 파일의 Git 제외 규칙을 포함합니다. 첨부 원본, 이미지 참고 자료, 미리보기, 추출 중간 결과와 서버 코드 백업은 실행 코드와 구분해 보존하며 운영 배포 파일에는 포함하지 않습니다. `.env.example`은 Git 내용 차이가 없습니다.

### 수정됨 — 통합 검증
- 캐시이동 테스트가 예전 `with discord.File` 동작을 기대하던 부분을 실제 파일 객체 전달·닫기 검사로 수정했습니다.
- 썬데이·공지 조회 테스트에서 별도 데이터 갱신 알림을 모의 처리해 실제 로컬 알림 상태 파일 접근과 테스트 실행 순서에 따른 실패를 막았습니다. 알림 자체의 기준 등록·중복 방지·실패 재시도는 전용 테스트로 검증합니다.
- README의 `/핫위크`·`/큐브세일` 미구현 설명, 제거된 패치 질문 안내, 이전 번역 제공자 설명과 요약 테스트 인증 안내를 현재 코드에 맞게 정리했습니다.
- 검증: 전체 단위 테스트 402개 통과, `git diff --check` 통과, 아이템 ID 중복 없음·아이콘 ZIP 무결성·아이콘 참조 누락 없음 확인. 서버 접속용 SSH 키 경로 확인 전이므로 이번 통합 서버 배포·재시작은 아직 수행하지 않았습니다.

### 추가됨
- `tools/test_patch_summary.py`를 추가했습니다. 별도 Discord 명령·봇 실행 없이 기존 GPT 요약과 Google 번역을 1회 테스트합니다. `.env`는 읽지 않고 실행 환경의 인증만 사용하며 자동 재시도·공지 전송·상태 저장은 하지 않습니다.
- 사용자 지정 영어 원문·기존 번역·권장 번역을 `TRANSLATION_GLOSSARY.md`에 정리했습니다. 관리용 문서만 추가했으며 봇 자동 적용·운영 교정 DB 변경·번역 API 호출은 수행하지 않았습니다.

### 변경됨
- 새 버전 패치노트·신규 스스비 캐시샵 항목에 대한 소유자 DM 갱신 안내를 로컬 구현했습니다. 최초 실행 기준 등록, 버전/공지별 성공 기록, 실패 재시도, 진행 중 판매 제외를 적용했습니다. 실제 DM 전송 및 운영 배포는 하지 않았습니다.
- 아이템검색 TSV·아이콘 ZIP을 새 Steam 클라이언트 추출 결과(45,207개 아이템·15,184개 아이콘)로 로컬 갱신했습니다. 신규 ID 866개, 기존 ID 영문명 변경 163개이며 이전 파일을 별도 백업했습니다. ZIP 무결성·아이콘 참조·중복 ID 검증과 알림/아이템검색/외형검색/스스비 테스트 24개를 통과했습니다. 운영 데이터는 아직 변경하지 않았습니다.
- `/연마석`을 처음 열 때 연마석 기본 선택 수를 5개에서 1개로 변경했습니다. 초기 선택 메뉴·이미지·사용 개수는 같은 값으로 표시합니다. 로컬 수정이며 운영 배포 전입니다.
- 기존 임베드 교정이 조사 `가·에`가 붙은 `럭스 사우나가·에렐 라이트에`를 놓치던 문제를 로컬 수정했습니다. 조사는 보존하며 다른 단어 일부는 교정하지 않습니다. 붙여 쓴 `럭스사우나`도 지원합니다. 기존 운영 임베드 재편집은 아직 수행하지 않았습니다.
- 패치 추가 수정 알림의 줄 앞 구분 제목을 `추가·변경·삭제`로 고정했습니다. 번역된 새 요약과 저장된 미전송 요약에 적용하며 본문·수치와 전송 이력은 유지합니다. 로컬 반영이며 운영 배포 전입니다.
- 업적 검사에서 전역 응답의 다른 월드 캐릭터를 DB 누락으로 오인하던 비교를 수정했습니다. 같은 날짜 스냅샷의 실제 월드로 검사 대상을 구분하고 미등록 이름·업적 값 누락은 계속 감지합니다. 로컬 테스트 26개 통과. 9월 8일 운영 자료를 수정 기준으로 읽기 전용 재검사한 결과 네 월드 모두 배치 대비 실제 업적 누락 0명입니다. 완료 표식은 여전히 0개로 미완료 경고는 별개이며, 기존 DB·보고서·알림은 변경하지 않았습니다. 운영 배포 전입니다.
- 사용자 지정 `Arteria → 아르테리아`, `Tallahart → 탈라하트`를 로컬 용어사전에 추가했습니다. 운영 서버 반영은 아직 수행하지 않았습니다.
- 승인 후 직업 버튼형 패치 수정 알림과 v271 요약 용어 교정을 운영 적용했습니다. 두 채널에서 패치 수정 알림 2개를 버튼형으로, 기존 요약 2개를 사전 용어로 편집했습니다. 기존 알림과 모든 피해량 수치가 일치하는지 검사 후 전환하고 원본을 보관했습니다. 서버 테스트 9개 통과, 봇 정상 실행·랭킹 수집기 유지 확인. 새 메시지는 보내지 않았습니다.
- `Collaboration → 콜라보레이션`을 로컬 사전에 등록하고 v271 요약 임베드의 기존 오역을 교정하도록 준비했습니다. 기어드락·퍼밀리어·어센틱심볼·코어·선택받은 세렌·카링·지구방위본부·리프·VIP 사우나 등을 포함합니다. 운영 반영은 승인 대기 중입니다.
- 기존 v271 피해량 수정 알림을 하야토·칸나·린·묵현 버튼으로 나누는 미리보기를 로컬 구현했습니다. 재시작 후 버튼 등록·원본 보관·피해량 일치 검사로 안전하게 기존 메시지만 편집합니다. 테스트 8개 통과. 보안 검토가 새 파일의 서버 전송을 차단해 운영 임베드는 아직 변경하지 않았습니다.
- 사용자 명시 승인 후 대기 중이던 보스명·버닝 필드·스킬명·얼굴 교정과 용어사전을 운영에 일괄 반영했습니다. 서버 테스트 7개 통과, 22:06:55 UTC Discord 연결 및 공지 채널 2곳에서 총 8개 기존 임베드 편집 완료 기록을 확인했습니다. 새 메시지는 보내지 않았고 랭킹 수집기는 유지했습니다. 캐시이동 변경은 이번 배포에 포함하지 않았습니다.
- `Face → 얼굴`을 로컬 용어사전에 추가하고 Signature Style Collection의 `페이스·페이스 쿠폰`을 `얼굴·얼굴 쿠폰`으로 교정하도록 준비했습니다. 앞선 교정과 함께 운영 반영 승인 대기 상태입니다.
- 묵현·스파이더 인 미러·크레스트 오브 더 솔라 등 사용자 지정 스킬명 8개와 `Burning Field → 버닝 필드`를 로컬 용어사전에 추가했습니다. 44415 이벤트·44597 패치 임베드의 교정을 준비하고 수치 유지·소유 표현·반복 실행 테스트를 포함해 17개를 통과했습니다. 앞선 보스명 교정과 함께 원격 업로드 승인 대기 중이며 기존 메시지는 아직 수정하지 않았습니다.
- `Guardian Angel Slime → 가디언 엔젤 슬라임`, `Vellum → 벨룸`을 로컬 사전에 추가하고 44414 이벤트 임베드 교정을 준비했습니다. 테스트 15개 통과. 원격 파일 전송이 보안 검토에서 차단되어 운영 사전·기존 임베드 반영은 아직 미완료입니다.
- `Linie → 리니에`, `Lügner → 류그너`를 운영 용어사전에 추가했습니다. Signature Style Collection 기존 임베드 2개를 교정하고 `프리에렌의·펀의` 소유 표현도 수정했습니다. 로컬 테스트 14개·서버 테스트 3개 통과, 새 공지 전송 없이 완료 기록을 확인했습니다.
- 원격 전송·재시작에 대한 사용자 명시 승인 후 필로소퍼 북 용어사전과 교정 코드를 운영 반영했습니다. 서버 테스트 2개 통과, 21:49:52 UTC Discord 연결과 공지 채널 2곳의 기존 임베드 수정 성공 기록을 확인했습니다. 새 메시지는 보내지 않았고 랭킹 수집기는 유지했습니다.
- `Philosopher's Book → 필로소퍼 북`과 `Frieren` 오역 사례 `프리에렌`을 사전에 추가하고, 44314 공지의 `철학자의 책·프리에렌·펀` 교정을 준비했습니다. 테스트 13개 통과. 원격 업로드가 자동 보안 검토에서 차단되어 운영 사전 반영과 기존 임베드 수정은 미완료입니다.
- 사용자 승인으로 기존 `Special Petite Luna Pets` 공지 임베드 2개(공지 채널 2곳)의 `펀 → 페른`, `스타크 → 슈타르크`를 수정했습니다. 새 공지는 보내지 않았고 완료 기록으로 반복 편집을 막았습니다. 20:35:06 UTC 운영 봇 연결을 확인했으며 미배포 캐시이동 변경은 포함하지 않았습니다.
- `Frieren → 프리렌`, `Fern → 페른`, `Stark → 슈타르크`, `Übel → 위벨`을 용어사전에 등록하고 사전 파일만 운영 반영했습니다(79개). 보호·복원 확인 및 용어사전 테스트 11개 통과. 기존 Discord 임베드 수정은 아직 수행하지 않았습니다.
- 캐시이동 종료 자동 알림을 추가했습니다. 종료 전 확인한 일정만 기존 알림 채널에 전송하며, 채널별 성공 기록을 보존하고 실패 시 재시도합니다. 이번에 이미 끝난 일정은 소급 전송하지 않습니다. 로컬 반영이며 운영 배포 전입니다.
- `/캐시이동`에서 종료 시각부터 지난 일정·이미지 대신 “현재 진행 중인 캐시이동 이벤트가 없습니다.”를 비공개로 안내하도록 수정했습니다. 진행 중·예정 일정과 자동 알림은 유지합니다. 로컬 반영이며 운영 배포 전입니다.
- 마지막 정상 캐릭터 이미지 캐시를 운영 배포했습니다. 로컬 테스트 276개와 서버 모의 테스트 1개 통과 후 봇만 재시작했으며, 13:16:45 UTC Discord 연결을 확인했습니다. 랭킹 수집기와 알림 설정은 유지하고 테스트 알림은 보내지 않았습니다. 이전 코드는 `/home/ubuntu/maple-image-cache-deploy-SiF0v9Dk/maple_bot.py.before`에 보관했습니다.
- 사용자 승인 후 랭킹 태그 개편·긴 태그 폭 조절, `/패치질문` 제거, 헬럭스 난이도·익스트림 검마를 운영 배포했습니다. 서버 테스트 64개 통과 후 공지 봇만 재시작했으며 랭킹 수집기는 유지했습니다. 원본 코드는 `/home/ubuntu/maple-tags-deploy-LQpUb4xT/before/`에 보관합니다.
- 랭킹 태그를 지역·단계별·패러디 후보로 개편하고 높은 후보 점수 순 3개, 부캐 맨 앞 규칙을 적용했습니다. 미정 세부 구간과 우선순위를 `RANKING_TAGS.md`에 기록했습니다. 긴 태그가 폭을 넘으면 뒤 태그가 사라지던 렌더링을 글꼴 자동 조절·최종 말줄임으로 교체하고 실제 이미지로 점검했습니다. 운영 미배포입니다.
- 사용자 요청으로 응답 지연·품질 문제가 있는 `/패치질문`을 슬래시 명령 등록과 일반 안내에서 제거했습니다. GPT + Google 공지 요약·번역과 패치 추가 수정 알림은 유지합니다. 운영 명령 목록 제거는 배포 후 동기화 시 반영됩니다.
- 사용자 요청에 따라 `/명령어` 선택 메뉴를 없애고 일반 명령어 전체를 분류별 한 화면에 표시하도록 변경했습니다. `/관리자`는 서버 관리자 전용으로 분리했으며 두 안내 모두 본인에게만 표시됩니다. 서버 관련 테스트 5개 통과 후 운영 배포·공지 봇 재시작을 수행했습니다. 랭킹 수집기는 그대로 유지했고 원본은 `/home/ubuntu/maple-help-deploy-HRtNaLxA/maple_bot.py.before`에 보관합니다.
- `/명령어`를 자주 쓰는 명령어 첫 화면과 분류 선택 메뉴로 정리했습니다. `/관리자`를 별도로 추가해 서버 관리자 권한을 실행 시 확인하고 알림·정보 채널 설정 안내만 비공개로 표시합니다. 기존 명령어 기능은 유지하며 운영 미배포입니다.
- 사용자 승인 후 용어사전 보호·복원, 최신 75개 용어, 요약 불릿·빈 줄 정리를 운영에 배포했습니다. 서버 테스트 35개 통과, 2026-09-09 10:58:05 UTC Discord 연결을 확인했습니다. 랭킹 수집기 PID는 유지됐고 기존 메시지·캐시는 재번역하지 않았습니다. 원본은 `/home/ubuntu/maple-glossary-deploy-NxvbbiCA/before/`에 보관합니다.
- 공지·패치 수정 요약과 별도 요약 테스트의 최상위 불릿을 `-`로 통일하고 항목 사이 빈 줄을 하나로 맞췄습니다. 이어지는 설명·하위 항목은 유지합니다. 로컬 변경이며 기존 메시지·저장 요약은 소급 수정하지 않습니다.
- 용어사전에 `Mount → 라이딩`을 추가하고 기존 표현 `탈것`을 기록했습니다. 로컬 변경이며 운영 미배포입니다.
- `/썬데이` 고정 번역·표현 보정에서 17개 용어를 사전에 추가했습니다. 스타포스·솔 에르다·몬스터 컬렉션·슈피겔라·헤이스트 피버 타임 부스터 등을 포함하며 수치·조건과 문장 숨김 규칙은 가져오지 않았습니다. 로컬 변경이며 운영 미배포입니다.
- 용어사전에 `Leap → 리프`, `Luxe Sauna → VIP 사우나`, `Sunny Sunday → 썬데이 메이플`을 추가했습니다. 기존 오역 표현도 함께 기록했으며 로컬 다음 번역부터 적용합니다. 운영 배포는 아직 하지 않았습니다.
- 요약 테스트 실행 환경에 키가 없으면 터미널에서 OpenAI·Google 키를 숨김 입력받도록 추가했습니다. 키는 환경 변수나 파일에 저장하지 않으며 빈 입력·취소·비대화형 실행에서는 API 요청 없이 종료합니다.
- 요약 테스트의 인증 누락 안내를 수정했습니다. 누락된 환경 변수 이름과 단독 실행 시 `.env`가 자동 로드되지 않는 이유를 표시하며, 인증 처리 방식은 변경하지 않았습니다.
- 별도 `test_patch_summary.py`에서 API 요청 전에 용어사전 파일을 검사하고 경로·등록 개수를 표시하도록 보완했습니다. 실제 번역은 봇과 같은 사전 보호·복원 함수를 사용합니다.
- Google 번역에 파일 용어사전과 DM 교정값을 연결했습니다. 영어 원문의 긴 용어부터 임시 표식으로 보호하고 결과에서 권장 한국어로 복원합니다. 대소문자·공백·아포스트로피·일반 s 복수형과 Box/Boxes를 처리합니다. 표식 누락·중복은 전송 오류로 처리합니다. 중의적인 Arcane/Eternal/Dawn/Pitched 단독어는 장비·세트 문맥이 명시된 경우에만 적용합니다. 별도 요약 테스트도 파일 사전을 사용합니다. 로컬 변경이며 실제 API 호출·운영 배포는 하지 않았습니다.
- 용어사전에 `Premium Surprise Style Box`와 `PSSB`의 권장 번역을 `스스비`로 추가했습니다. 기존 표현은 `프리미엄 서프라이즈 스타일 박스`로 기록했습니다. 로컬 추가이며 Google 공지 번역에는 자동 적용되지 않습니다.
- 용어사전에 `Night Troupe → 나이트 트루프`를 추가하고 `나이트 트룹`을 오역 표현으로 기록했습니다. 이 항목은 로컬 추가이며 Google 공지 번역에는 자동 적용되지 않습니다.
- 사용자 승인 후 GPT + Google 공지 복원과 용어사전·샤이닝 표시 코드를 메인 서버에 적용했습니다. 서버 테스트 31개 통과, 공지 봇만 재시작 후 2026-09-09 10:08:45 UTC Discord 연결을 확인했습니다. 랭킹 수집기는 재시작하지 않았습니다. 원본 코드 백업은 `/home/ubuntu/maple-news-restore-a3ZaLU3p/before/`에 보관합니다. 기존 저장 일정·메시지는 재번역하지 않았습니다.
- 공지 요약을 기존 `gpt-5.6-luna` 영어 요약 → Google 한국어 번역으로 복원했습니다. 패치 추가 수정 알림과 썬데이·캐시샵 번역도 이 제공자를 사용합니다. `/패치질문`의 Ollama와 용어사전·샤이닝 표시는 보존하며 교정표는 Google 번역에 자동 적용하지 않습니다. OpenAI 의존성·키 설정 예시를 복원했습니다. 운영 배포·재시작 및 실제 API 호출은 수행하지 않았습니다.
- 썬데이의 같은 날짜에 `21성 이하 파괴 확률 30% 감소`와 `스타포스 강화 비용 30% 할인`이 함께 있으면 기존 반짝이 이모지와 `스페셜: 샤이닝 스타포스` 표시를 붙입니다. 기존 스페셜 표기도 유지합니다. 로컬 변경이며 운영 배포·기존 저장 일정 갱신은 아직 수행하지 않았습니다.
- `TRANSLATION_GLOSSARY.md`의 확정 용어를 Ollama 요청에 자동 연결했습니다. 원문에 있는 용어만 전달하고 대소문자·일반 복수형·아포스트로피 차이와 긴 용어 우선을 처리하며, 동일 원문의 DM 교정값을 우선합니다. 추가 API 호출·기존 결과 치환은 하지 않습니다. 현재 로컬 검증 단계이며 운영 배포는 별도 승인 후 진행합니다.

### 수정됨
- 캐릭터 이미지 다운로드 실패 시 마지막 정상 이미지를 사용하도록 보완했습니다. 월드·닉네임별로 `character-images.db`에 저장하여 URL 변경·봇 재시작 후에도 복원하며, 잘못된 이미지로 기존 캐시를 덮지 않습니다. 첫 성공 전에는 이전 이미지가 없어 빈칸을 유지합니다. 로컬 변경이며 운영 미배포입니다.
- 후속 요청에 따라 표기는 `헬럭스`로 유지하고 제목은 `헬럭스 5%`, 난이도 선택은 `헬`로 정리했습니다. `일반`이나 중복 `헬`은 제목에 붙이지 않습니다.
- `/5퍼`의 보스명·난이도를 `일반 헬럭스`에서 `헬 골럭스`로 수정하고, 사용자 제공 표의 익스트림 검마(체력 4.80Q / 5% 240T)를 추가했습니다. 기존 K 단위 표시와 보스 이미지는 유지합니다. 로컬 변경이며 운영 미배포입니다.
- 요약 테스트의 번역 오류를 단계별로 구분합니다. Google 응답 형식·개수·빈 번역, 용어 표식 누락/변경·중복을 고정 안내문으로 표시하며 키와 응답 원문은 출력하지 않습니다. 실제 실패 원인은 다음 테스트 로그로 확인해야 하며 실제 API 재호출은 하지 않았습니다.
- 분산 유니온·업적 수집 진행 위치를 UTC 기준일별로 구분합니다. 같은 날 재시작은 이어서 수집하고, 새 기준일에는 각 샤드의 첫 페이지부터 시작합니다. 기준일을 넘겨 도착한 대표 랭킹 응답은 새 날짜의 중간 페이지로 저장하지 않고 다시 요청합니다.
- 경험치 기록보다 먼저 도착한 유니온·업적 값을 DB에 대기 보관하고 같은 날짜의 경험치가 도착하면 함께 반영합니다. 가짜 경험치 기록을 만들지 않으며, 늦게 도착한 과거 대표 배치가 더 최신 기준일의 캐릭터 현재값을 덮지 않도록 했습니다.
- 로컬 관련 테스트 287개와 메인 서버 회귀 테스트 35개를 통과한 수정본을 사용자 승인 후 메인 봇·수집기 4대에 배포하고 재시작했습니다. 운영 DB 전체 백업·무결성 검사 후 보존 원본을 근거로 2026-09-08 Kronos 26명의 스냅샷 유니온 값·순위를 복구했습니다. 기존 검사 보고서와 수집 원본은 그대로 보존합니다.

## 2026-09-08

### 추가됨
- `/패치질문` 입력창에서 최신 공식 패치노트를 근거로 Nemotron 답변을 받습니다. 원문 근거 문장을 검사하며 확인 불가·응답 실패를 구분하고 질문 쿨다운을 적용합니다.
- 최신 패치 본문을 5분마다 비교해 기존 공지 채널에 `패치노트 추가 수정` 알림을 보냅니다. 최초 조회는 기준만 저장하고 채널별 성공 기록과 생성한 요약을 보관해 재시작·일부 전송 실패 때 재사용합니다.
- 봇 소유자 전용 DM `번역교정`을 추가했습니다. 원문·기존 번역·원하는 표현을 확인 버튼으로 저장하고 목록·삭제를 지원합니다. 교정 DB는 재시작 후에도 유지되며 해당 원문이 포함된 새 Nemotron 요약·번역에만 반영합니다. 기존 발송 메시지는 변경하지 않습니다.

### 변경됨
- 썬데이의 `Meso`·`메조` 표기를 `메소`로 통일하고, 요청한 스타포스 할인 제외 문장과 중복된 몬스터 파크 익스트림 제외 문장을 숨깁니다. 기존 `(익몬 제외)` 표기는 유지합니다.
- 공지 한국어 요약과 썬데이·캐시샵의 API 번역을 Ollama Cloud `nemotron-3-ultra`로 통합했습니다. 요약 재번역을 없애고 120초 제한 및 번역 JSON 개수·형식 검증을 추가했습니다. 기존 고정 번역은 유지하며 OpenAI·Google 번역 키는 더 이상 요구하지 않습니다.
- 썬데이가 이전 패치 기준으로 남아 있으면 이미 처리한 최신 패치에서도 5분마다 재추출하도록 수정했습니다. 정상 추출 시 목록을 갱신하고 같은 날짜의 당일 알림 기록을 보존하며, 추출 실패 때는 기존 일정을 유지합니다.
- `/연마석` 생명은 최대 10개·개당 10%, 신념은 최대 20개·개당 5%로 개수 선택과 검증을 확장했습니다. 각 최대치에서 100% 성공하며 레벨 변경 시 초과 개수를 조정합니다. 6개 이상은 기존 5개용 이펙트만 재사용합니다.
- 캐시이동 종료 24시간 전 알림의 이미지 파일 처리 오류를 수정했습니다. 전송 성공·실패 후 파일을 직접 닫고 성공한 채널만 전송 완료로 기록합니다.
- `/연마석`은 초기의 같은 메시지 GIF 재생→정적 결과 전환→버튼 활성화 방식으로 복원하고, GIF 초반 준비 화면 0.5초만 유지했습니다. 결과 문구·누계·조건 선택 표시·만료 비활성화도 복원했습니다.
- `/연마석` GIF 시작에 준비 화면 0.5초를 추가했습니다. 조작 잠금 시간에도 포함하며 마지막 결과 화면 2초와 첨부파일만 교체하는 방식은 유지합니다.
- `/연마석` 메시지 생성·삭제 방식을 중단하고 고정 메시지의 첨부파일만 교체하는 시험판으로 변경했습니다. 문구·버튼과 종료 후 화면은 수정하지 않으며, GIF 앞 정지 구간 추가는 이번 시험 결과에 따라 별도로 진행합니다.
- `/연마석`은 메시지 수정 대신 새 화면 전송 후 이전 시뮬레이터 메시지를 삭제하는 시험판으로 변경했습니다. GIF 종료·만료 때도 메시지를 수정하지 않으며 재생 중 조작은 내부에서 차단합니다. 전송 실패 시 이전 화면을 보존합니다.
- `/연마석` 연속 강화 시 새 GIF를 올리기 전에 같은 메시지의 기존 GIF를 준비 PNG로 교체하는 시험판을 적용합니다. 첫 강화와 결과·버튼 갱신 방식은 유지하며 실제 재재생 해소 여부를 확인합니다.
- `/연마석` 이미지와 조작창을 한 메시지로 합쳤습니다. 이펙트 종료 시 GIF 첨부를 그대로 유지하고 결과 문구·버튼만 갱신하며 마지막 결과 화면 2초와 중복 클릭 방지를 유지합니다. Discord에서 재재생되는지는 사용자 환경에서 추가 확인합니다.

## 2026-09-07

### 수정됨
- `/연마석` 위·아래 패널 접합부에 남아 있던 둥근 모서리를 제외해 양쪽 외곽선이 중간에서 끊겨 보이지 않도록 수정했습니다.
- `/큐브세일`의 얼음 이모티콘을 제거하고 제목 왼쪽에 기존 큐브 이미지를 표시합니다.
- `/큐브세일`·`/핫위크` 결과 하단에서 캐시 시간·원문 언어·표시 개수에 관한 불필요한 안내 문구를 제거했습니다.
- `/패치`가 공지·썸네일 다운로드 전에 대기 응답을 보내도록 수정했습니다. 공지 조회 실패는 오류 안내를 보내며 이미지 다운로드 실패 시에도 제목과 링크를 제공합니다.
- Python 3.10의 `asyncio.TimeoutError`를 명시적으로 처리해 `/서버`·`/스스비`와 재추첨, 이미지 다운로드, 환율 갱신, 랭킹 수집·백필 제어의 기존 오류 처리가 동작하도록 보완했습니다.
- `/랭킹`이 캐시된 과거 프로필로 날짜별 수집 기록을 덮어쓰지 않도록 조회와 저장을 분리했습니다. 새 공식 조회만 기록하며 카드의 레벨·경험치·전체 순위는 최신 스냅샷과 실제 기준일로 표시합니다. 기준일이 없는 기존 프로필은 확인 불가로 안내하고, 수집값과 달라진 캐시의 월드 순위는 표시하지 않습니다.

### 변경됨
- `/연마석` 이미지와 조작창을 별도 메시지로 분리했습니다. 정상 GIF는 재생 종료 시 다시 수정하지 않고 조작창만 활성화해 메시지 갱신에 따른 재재생을 줄입니다. 조건 변경·다음 강화 때 이미지를 갱신하며 생성 실패 시 정적 결과로 대체합니다.
- `/연마석` GIF 마지막에 기본 결과 화면 2초를 추가했습니다. Discord 갱신 지연 중 이펙트 재반복을 줄이되 버튼은 이펙트 종료 시점에 활성화하고 정적 이미지로 교체합니다. 추가 2초는 버튼 대기에 포함하지 않습니다.
- `/연마석`의 이펙트 종료 후 0.4초 정지 구간을 제거했습니다. 원본 이펙트 재생 시간만 기다린 뒤 정적 결과로 전환합니다.
- `/연마석`을 원본 패널 비율의 418×342 화면과 Noto Sans KR 글꼴로 변경했습니다. 이펙트 종료 후 0.4초 유지한 뒤 정적 결과로 전환하며 기존 중복 클릭 방지와 결과 보존 처리는 유지합니다.
- `/연마석` 강화 버튼에 실제 연마·성공/실패 GIF를 연결했습니다. 재생 중 중복 강화와 조건 변경을 막고 종료 후 정적 결과로 바꿉니다. GIF는 별도 스레드에서 생성·재사용하며 실패해도 재추첨 없이 같은 결과를 표시합니다.
- `/연마석` 이미지를 600×380 가로형 화면으로 변경했습니다. 강화 전후 반지·레벨과 연마석 개수를 크게 표시하고 청회색 그라데이션을 적용하며 누계는 메시지 텍스트로 유지합니다. GIF 연출은 포함하지 않습니다.
- `/시드링`을 `/연마석`으로 변경하고 준비·성공·실패 이미지를 표시합니다. 레벨·개수는 드롭다운으로 선택하며 강화 버튼을 눌러야 독립 추첨합니다. 조건을 바꿔도 실제 사용 연마석 누계를 유지하고 10분 미사용 시 비활성화됩니다.
- `/5퍼`의 직접 입력 옵션을 보스·난이도 드롭다운으로 변경했습니다. 기본 검밑 화면에서 선택하면 같은 메시지의 수치·이미지가 갱신됩니다. 실행자만 조작할 수 있고 10분 미사용 시 비활성화됩니다.
- `/헥사`·`/성장의비약`·`/에픽던전`·`/심볼계산기`의 명령어 뒤 입력 옵션을 제거하고 드롭다운·수치 입력창·계산 버튼으로 변경했습니다. 본인 전용 메시지에서 반복 계산할 수 있으며 미사용 10분 후 비활성화됩니다. 기존 계산 규칙과 심볼 보약·엘라노스 선택값 저장은 유지합니다.
- `/랭킹`은 현재 기준일에 아직 수집되지 않은 캐릭터만 공식 최신 조회를 기다리도록 변경했습니다. 대기 안내 후 최대 45초 동안 조회하고, 실패하면 기존 기록·실제 기준일과 실패 안내를 표시합니다. 같은 캐릭터의 동시 요청을 공유하고 대기 중 수집기가 저장한 당일·이후 기록은 보호합니다.
- 랭킹 스냅샷의 무조건 30일 삭제를 제거하고 최근 90개 기준일 유지·그 이전 월별 gzip JSONL 보관으로 변경했습니다. 다른 파일시스템의 복사본까지 해시·행 수·SQLite 복원 검증을 통과한 값만 운영 DB에서 정리하며, 복사 위치 미설정이나 실패 시 원본을 유지합니다.
- 보관된 캐릭터의 최초 관측일을 운영 DB에 유지하고, 늦게 도착한 과거 정정 기록은 새 묶음에 추가합니다. 압축본을 새 조회용 SQLite DB로 복원하는 `python -m ranking_archive` 도구를 추가했습니다.

### 추가됨
- `/알림설정확인`으로 서버 관리자가 현재 서버의 알림 채널·서버 오픈 멘션 역할·시간 및 환율 정보 채널 설정을 비공개로 조회할 수 있습니다. 다른 서버의 설정은 표시하지 않으며 기존 설정을 변경하지 않습니다.
- `/핫위크`·`/큐브세일`을 공식 공지 조회에 연결했습니다. 진행 중·예정 일정, 보상/판매 내용과 원문 링크를 표시하고 종료된 일정은 제외합니다. 일반 큐브 할인·할인 패키지만 다루고 특수 큐브 기간 한정 판매는 제외합니다. 조회 결과는 5분간 메모리에 재사용하며 조회·날짜 해석 실패를 이벤트 없음과 구분합니다.
- `/exp쿠폰`을 옵션 없이 실행하면 쿠폰·버닝 드롭다운, 레벨·경험치·개수 입력창, 계산 버튼을 제공합니다. 본인 전용 설정창에서 반복 계산할 수 있고 미사용 10분 후 비활성화됩니다. 기존 직접 입력 방식은 유지하고 쿠폰 자동완성이 실제 Discord의 `시작레벨` 값을 읽도록 수정했습니다.
- 캐시이동 종료 알림은 `/캐시이동`과 동일한 파란색 임베드·캐릭터 이미지·시작/종료 시각·참여 안내를 사용하고 제목에 `종료 임박`을 표시합니다.
- 기존 알림 채널에 캐시이동 종료 24시간 전, 미라클 타임 종료 1시간 전 알림을 추가했습니다. 종료 전 재시작 시 미전송 알림을 보내고 채널별·종료 시각별 전송 기록을 보존해 중복을 방지합니다. 패치노트에서 종료 시각이 변경되면 새 종료 시각 기준으로 다시 알립니다.
- 분산 랭킹 수집 배치에 빈 페이지와 월드·종류·샤드별 완료 표식을 기록합니다. 메인 봇은 5분마다 검사 준비 상태를 확인하고 경험치·유니온·업적 단계별로 누락 페이지, 중복 캐릭터, DB 반영 누락, 직전 정상 검사 대비 10% 이상 인원 감소를 검사합니다.
- 이상이 있거나 다음 기준일 17:10 UTC까지 완료 표식이 모이지 않으면 봇 소유자에게 DM을 보냅니다. 검사 결과와 전송 성공 여부는 DB에 보존하며 정상 결과는 로그에만 기록합니다. 자동 데이터 수정이나 재수집은 수행하지 않습니다.

## 2026-09-05

### 추가됨
- 봇 소유자가 Discord DM으로 `랭킹 상태` 또는 `랭킹 확인`을 보내면 기준일과 UTC 시작 시각, 저장 인원·진행률, 예상 종료 또는 실제 완료 시각·총 소요 시간, 수집기별 현재 위치와 실패 배치를 확인할 수 있습니다.
- `/도핑`과 `!도핑`으로 GMS 보스 물약·스킬·버프 도핑 가이드 이미지를 임베드 없이 확인할 수 있습니다.

### 변경됨
- 메인 1대와 보조 3대가 Scania·Bera·Kronos·Hyperion의 페이지를 네 샤드로 나눠 수집하고, 경험치 랭킹 완료 후 Lv.260 이상 대표 캐릭터의 유니온·업적 랭킹을 이어서 수집하도록 운영 구성을 정리했습니다.
- 메인 Discord 봇은 분산 수집기의 완성된 JSONL만 `ranking.db`에 합치며 자체 전체 순회는 import-only 모드로 실행합니다.
- `/랭킹` 카드의 누적 경험치 영역을 다음 레벨까지 남은 경험치와 최근 7일 평균 기준 예상 소요일로 변경했습니다. 7일·14일·30일 일평균은 해당 기간 기록이 모두 있을 때만 계산합니다.
- 랭킹 그래프에 기록 기간 제목과 위쪽 여유를 추가하고, 레벨이 오른 날짜에는 경험치 값 위에 레벨 달성 배지를 표시합니다. Lv.300 캐릭터는 최고 레벨 달성 문구를 표시합니다.
- 랭킹 카드의 작은 글자를 모바일에서 읽기 쉽도록 키우고, 이전 닉네임과 긴 순위 문구가 카드 폭을 넘으면 말줄임표로 처리합니다. 상위 백분율은 값 크기에 따라 소수 자릿수를 조절합니다.
- 랭킹 카드의 태그를 항상 3개 표시하고 각 태그 자체의 기준 점수에 따라 레전더리·유니크·에픽·레어·커먼 단계 색상의 캡슐 배경을 사용합니다.
- `/랭킹`과 `/닉네임추적`의 닉네임 입력을 메이플스토리 기준 최대 12바이트로 제한합니다. 한글은 2바이트, 영문·숫자는 1바이트로 계산합니다.
- `/랭킹`이 실시간 추가 요청 없이 전체 수집기에 저장된 유니온·업적 대표 캐릭터 값을 캐시된 프로필과 합쳐 사용하도록 변경했습니다.
- 진행 인원이 직전 수집일 기준을 넘어도 인원수만으로 완료 처리하지 않고, 네 수집기가 모두 유니온·업적 단계로 전환된 뒤에만 100%와 완료 시각을 표시합니다.

### 수정됨
- 이미 전달한 백필 오류 파일을 비워 서버 재시작 때 같은 오류 DM이 반복되지 않게 했습니다.
- 대표 캐릭터임에도 캐시된 프로필에 유니온·업적이 없어 `대표 캐릭터에서만 확인 가능`으로 표시되던 문제를 수정했습니다.
- 새로 관측된 캐릭터를 닉네임 변경으로 연결하지 않고, 변경 기록이 없을 때 첫 관측일과 Lv.260 이상 신규 진입·월드 리프·랭킹 재등장 가능성을 안내하도록 구분했습니다.

## 2026-09-02

### 추가됨
- `/패치`와 `!패치`로 1분마다 갱신되는 최신 공식 패치노트의 제목·링크·사이트 썸네일을 확인할 수 있습니다.
- `/시간`과 `!시간`으로 UTC와 주요 지역의 현재 시각을 서머타임이 자동 반영된 임베드로 확인할 수 있습니다.
- `/항해`와 `!항해`로 GMS 항해 가이드 이미지를 바로 확인할 수 있습니다.

### 변경됨
- 새 공지 목록 확인 주기를 5분에서 1분으로 줄이고, 기존 패치노트와 진행 중인 점검 공지 본문 재확인은 5분 간격을 유지했습니다.
- 퍼밀리어 시뮬레이터 결과를 임베드 없이 카드 이미지와 일반 메시지로 표시합니다.

## 2026-09-01

### 추가됨
- 연속된 완전 수집일의 월드·직업·레벨·경험치·순위를 비교해 닉네임 변경 후보를 찾고 `/닉네임추적`으로 확인할 수 있게 했습니다.

### 변경됨
- 메인·보조 랭킹 수집기가 1초당 1페이지 요청 시작 제한은 유지하면서 느린 API 응답을 최대 3개까지 겹쳐 기다리도록 바꿔, 403·429 위험을 늘리지 않고 실효 수집 속도를 높였습니다.
- 1.4GB 랭킹 DB 백업이 수집 속도를 떨어뜨리지 않도록 일일 수집 완료 직후 한 번 자동 실행하고, 백업 자체도 봇 응답과 분리된 작업으로 처리합니다.
- 순차 수집 때 이미 갱신된 활성 페이지 전체를 매번 읽던 DB 조회를 현재 페이지 기본키 확인으로 줄여 봇 응답 지연과 수집 병목을 제거했습니다.
- 랭킹 수집 최초 시작 시각을 기준일 17:10 UTC로 고정해 재시작 후에도 바뀌지 않게 하고, 완료 로그에 UTC 시작·종료 시각과 총 소요 초·시간을 기록합니다.
- MapleBot 백필이 화면 제목 대신 실제 경험치 그래프를 최대 5초만 기다리도록 바꾸고, 연속 장애 복구 대기를 10초·30초·90초로 줄여 실패 구간의 낭비 시간을 단축했습니다.
- MapleBot에 없는 캐릭터가 연속되어도 사이트 장애로 오인해 브라우저를 재시작하거나 복구 대기하지 않도록 분리했습니다.
- 일일 랭킹 스냅샷에 월드·직업과 대표 캐릭터의 유니온 레벨/순위·업적 점수/순위를 함께 보존합니다.
- 실제 랭킹 DB 연결마다 WAL의 `synchronous=NORMAL`을 적용하고, 일반 페이지 저장의 불필요한 미래 기록 삭제 쿼리와 배치마다 반복되던 30일 보관 정리를 제거했습니다.
- 랭킹 카드의 모집단 3개를 DB 연결 한 번으로 읽고 누적 경험치 합계를 미리 계산했습니다. 일일 모집단·활성 페이지 준비 완료 여부도 메모리에 기억해 초당 반복되던 DB 확인을 없앴습니다.
- MapleBot 백필은 이미지·폰트·미디어 다운로드를 생략하고, 정상 프로필에 경험치 그래프가 없는 캐릭터를 두 번 확인한 뒤 체크포인트에 기록해 재시작 후에도 반복 조회하지 않습니다.
- 그래프가 5초 제한과 동시에 나타나 Playwright가 타임아웃을 먼저 반환해도 DOM에 생성된 그래프를 바로 추출합니다.
- MapleBot 백필이 늦게 그려지는 그래프를 해석하지 않고 공개 캐릭터 API의 원본 경험치 기록을 브라우저에서 직접 읽도록 변경했습니다.
- 현재 사용되지 않는 기존 `/랭킹` 임베드 생성 코드와 전용 테스트를 삭제했습니다.
- 이미 제거된 `/서버랭킹`·`/랭킹등록`·`/랭킹해제`에서 남아 있던 서버별 캐릭터 등록 테이블 생성 코드와 저장소 메서드·테스트를 삭제했습니다.
- 백필 결과 반영 트랜잭션의 커밋 단위를 5명에서 50명으로 늘려 대량 스냅샷 입력 시간을 줄였습니다.

## 2026-08-31

### 수정됨
- 랭킹 백필 중 MapleBot 페이지가 일시적으로 늦게 열리면 실패 캐릭터를 다시 대기열에 넣고 브라우저를 재생성한 뒤 30초·2분·5분 간격으로 자동 복구합니다. 반복해서 검색되지 않는 캐릭터는 건너뛰며, 실제 차단이나 장시간 복구 실패만 수집기를 중단하고 관리자에게 알립니다.
- 백필 실패 캐릭터를 한 자리에서 최대 3회 기다리는 대신 8초 확인 후 대기열 끝에서 다시 시도하도록 바꿔, 정상 요청 간격을 유지하면서 일시적인 페이지 지연 구간을 빠르게 통과합니다.
- 봇 소유자가 Discord DM으로 `백필 상태`·`백필 확인`을 보내면 체크포인트 진행량을 확인하고, `백필 재시작`을 보내면 보조 서버 수집기를 이어서 시작할 수 있습니다. 세 문구는 띄어쓰기 없이 보내도 인식합니다.
- 백필 수집기가 오류로 종료되면 현재 레벨을 기억해 같은 체크포인트에서 5초 후 자동 재실행합니다.

## 2026-08-30

### 추가됨
- `/ㅁ`과 같은 복사용 문구를 표시하는 `/심볼` 및 `!심볼` 별칭을 추가했습니다.
- `/랭킹` 기록일을 00:10 UTC 경계로 통일하고 이미지 하단에 마지막 기록 날짜를 표시합니다.
- 잘못 미래 날짜로 저장된 랭킹 기록은 다음 수집 시 정상 날짜로 자동 보정합니다.
- `메창력` 표시를 `메이플 종합 지수`로 순화하고 랭킹 이미지에 최대 3개의 태그를 표시합니다.
- 신뢰할 비교 기준이 부족한 `/서버랭킹`·`/랭킹등록`·`/랭킹해제` 명령어를 제거했습니다.
- `/랭킹` 실행 중 유니온·업적 API를 기다리지 않고 저장된 대표 캐릭터 데이터만 사용하며, 두 정보가 없으면 `부캐` 태그와 대표 캐릭터 전용 안내를 표시합니다.

## 2026-08-29

### 변경됨
- `/랭킹` 경험치 그래프를 짙은 청회색 바탕, 하늘색 그래프, 라임 포인트 색배합으로 변경했습니다.
- `/랭킹` 경험치 그래프 왼쪽에 최고 경험치에 따라 자동 조절되는 `T` 단위 기준 눈금을 추가했습니다.
- 경험치 그래프의 상단 설명 문구를 제거하고, 각 날짜의 획득 경험치를 어두운 캡슐과 밝은 글자로 강조했습니다.
- `/랭킹`의 임베드를 없애고 캐릭터 이미지·레벨·직업·월드·전체/월드/유니온/업적 순위·메창력·칭호와 14일 경험치 그래프를 한 장의 이미지 카드로 통합했습니다.
- 랭킹 이미지에 현재 레벨 경험치/필요 경험치와 7일·14일·30일 기준 일평균·누적 획득 경험치 요약 패널을 추가했습니다.
- 메창력 영역을 작은 라벨·강조 숫자·칭호 구조로 재배치해 한글 라벨이 과도하게 커 보이던 문제를 수정했습니다.
- `/채널추천`이 Discord 닉네임의 마지막 한글 음절 받침에 따라 `아`와 `야`를 구분하도록 변경했습니다.

## 2026-08-27

### 변경됨
- `/랭킹`으로 조회한 캐릭터를 다음 날 자동 수집의 최우선 대상으로 갱신한 뒤 기존 월드별 상위 랭커 순서를 이어갑니다. 같은 캐릭터는 중복 등록하지 않으며 모든 랭킹 요청이 공통 1초 간격 제한을 공유합니다.
- 최근 7일 안에 레벨·경험치가 변한 캐릭터가 포함된 랭킹 페이지를 우선 갱신하고, 7일 이상 페이지 전원이 그대로인 구간은 마지막 순차 수집으로 미룹니다. 7일치 기록이 아직 없는 캐릭터도 우선 대상에 포함합니다.
- GMS 공홈 랭킹의 실제 일일 갱신 시각을 하루 동안 측정하는 임시 도구를 추가했습니다. 최근 경험치가 변한 캐릭터 5명의 레벨·경험치·순위를 10분마다 비교하고 최초 변경 감지 시각을 한국시간으로 파일에 기록합니다.
- `/랭킹`으로 Lv.260 미만 캐릭터를 조회하면 데이터가 없다고 안내하고 결과 저장과 자동 수집 우선순위 등록을 하지 않습니다.
- `/랭킹`의 월드 순위 옆에 해당 월드 전체 등록 인원을 기준으로 계산한 상위 백분율을 소수점 넷째 자리까지 표시합니다.
- GMS 랭킹 수집은 매일 상위 랭커부터 다시 시작해 약 6시간 안에 최대 약 21만 명을 우선 갱신하고 이후 하위 순위를 계속 수집합니다. 403은 실제 서버 로그에 맞춰 5분·15분·1시간·최대 6시간, 429는 1분부터 최대 1시간까지 단계적으로 대기하며, 대기 상태를 DB에 저장해 서비스 재시작 후에도 차단 요청을 반복하지 않습니다.
- Scania·Bera·Kronos·Hyperion의 월드별 상위 랭킹을 1초마다 1페이지(최대 10명)씩 번갈아 수집하도록 조정했습니다. 20명/초와 15명/초는 단시간에, 12.5명/초는 약 9시간 뒤부터 403 제한이 반복됐습니다. 장기 운용 후보인 10명/초로 되돌렸습니다.
- 랭킹 API 제한 대기를 수집 함수 내부의 긴 정지 대신 재개 시각 확인 방식으로 변경해, 5분 뒤 밀린 요청이 한꺼번에 전송되지 않도록 수정했습니다.
- `/랭킹` 경험치 그래프의 안내 문구를 수동 조회 기록이 아닌 공식 랭킹 수집 기록으로 바로잡았습니다.
- 랭킹 경험치 기록 보관 기간을 15일에서 30일로 늘렸습니다. 기본 그래프는 기존처럼 최근 14일만 표시하며, 이후 7일·30일 보기 기능을 추가할 때 사용할 수 있습니다.
- 크로노스 Lv.260 이상 랭킹을 명령어 조회와 별개로 5초마다 10명씩 자동 수집하도록 변경했습니다. 날짜가 바뀌어도 이전 순번부터 이어가며, Lv.260 미만 경계까지 한 바퀴를 마치면 다시 상위 랭커부터 갱신합니다. 공식 API가 429 제한을 보내면 안내된 시간만큼 기다린 뒤 같은 순위부터 재개합니다.
- 랭킹 수집 데이터는 페이지마다 저장하되, 별도 SQLite 백업은 1시간마다 만들도록 조정했습니다.
- `/랭킹`에 유니온·업적 점수와 순위를 추가하고, `/랭킹등록`·`/랭킹해제`·`/서버랭킹`으로 Discord 서버별 등록 캐릭터만 비교할 수 있게 했습니다.
- 서버 랭킹에 레벨·경험치·유니온·업적을 종합한 0.00~99.90점의 재미용 `메창력`과 분야별 칭호를 추가했습니다. 세 랭킹이 모두 1위면 99.90점입니다.
- `/서버랭킹 닉네임:캐릭터명`으로 메창력 순위에서 해당 캐릭터와 위·아래 최대 5명만 확인할 수 있게 했습니다.
- `/랭킹`에서 처음 직접 조회한 캐릭터를 Discord 사용자별 기본 캐릭터로 기억해, 이후에는 닉네임 없이 `/랭킹`만 입력해도 다시 조회할 수 있게 했습니다.

### 수정됨
- 점검 중 공식 서버 상태 API가 월드 목록을 비워 보내는 경우를 서버 접속 불가 상태로 처리해, 서버가 다시 열렸을 때 오픈 알림을 감지하도록 수정했습니다.
- 넥슨 업적 랭킹 API의 실제 점수 필드인 `starSum`을 읽도록 수정해 `/랭킹`과 `/서버랭킹`에 업적 점수가 표시되게 했습니다.

## 2026-08-20

### 추가됨
- `/퍼밀리어`와 `/스스비` 결과를 같은 메시지에서 다시 추첨하는 실행자 전용 `리롤` 버튼을 추가했습니다.
- `/스스비`를 `/ssb`로 검색하는 명령어 현지화와 초성 별칭 `/ㅅㅅㅂ`을 시험 적용했습니다.
- `/퍼밀리어`와 `/스스비`에 누적 횟수와 실행자 전용 `기대값 계산하기` 버튼을 추가했습니다.
- 퍼밀리어 전체 4,784개 조합의 확률·평균 기대 횟수·실제 출현 확률 기준 희귀도를 `familiar.db`에 미리 저장하고 버튼에서 조회하도록 했습니다.
- 퍼밀리어와 PSSB 기대값에 내 누적 시행 횟수 안에 현재 결과를 한 번 이상 얻을 확률을 추가했습니다.

### 변경됨
- `/스스비` 리롤 때마다 최신 공식 확률표를 다시 읽어 캐시샵 품목 변경을 자동 반영하도록 했습니다.
- 2.00% 이하 PSSB 보상의 네모 표식을 `✨`로 변경했습니다.
- `/퍼밀리어`와 `/스스비`의 `리롤` 버튼 이름을 `다시 뽑기`로 변경했습니다.
- `/퍼밀리어`와 `/스스비` 버튼의 유효시간을 15분에서 24시간으로 연장했습니다.
- `/퍼밀리어`의 `기대값 계산하기` 버튼은 다른 사용자도 누를 수 있도록 변경했습니다.
- `/퍼밀리어`와 `/스스비`의 `다시 뽑기` 버튼에서 주사위 이모지를 제거했습니다.
- `/스스비` 기대값에 낱개와 11개 세트의 최저가 조합으로 계산한 평균 NX 비용을 추가했습니다.
- `/스스비` 누적 횟수 아래에 지금까지 사용한 NX를 표시하도록 변경했습니다.

### 수정됨
- 구현되어 있지만 등록 목록에서 빠졌던 `/외형검색` 명령어를 Discord에 등록했습니다.
- 이름이 중복된 `Hangover Make-up`이 아이콘 없는 성형 데이터와 연결되던 문제를 수정했습니다.
- PSSB 아이콘 매칭 후보에서 헤어와 성형 데이터를 제외했습니다.

## 2026-08-16

### 추가됨
- 리스트레인트 링 Lv.4·Lv.5 강화 결과를 추첨하는 `/시드링` 명령어와 같은 조건 반복 버튼을 추가했습니다. 반복 결과는 메시지 하나에서 누적하며 실행자만 버튼을 사용할 수 있습니다.
- 넥슨 공식 북미 GMS 랭킹에서 캐릭터의 레벨, 경험치 진행률, 전체·월드 순위와 유니온 정보를 보여주는 `/랭킹` 명령어를 추가했습니다.
- 크로노스 랭킹을 높은 레벨부터 10명씩 순차 수집하는 상위 1,000명 시험 작업을 추가했습니다. 기본값은 비활성화이며 `RANKING_SCAN_ENABLED=true`일 때만 동작합니다.
- 랭킹 기록을 SQLite `ranking.db`에 저장하고 페이지마다 재개 순번을 남기며, 수집 완료 후 프로젝트 밖 별도 위치에 무결성 검사를 거친 백업을 생성하도록 추가했습니다.
- 프로젝트 현재 버전을 `v1.4.3`으로 지정했습니다.
- `/ㅁ`에서 자주 쓰는 네 문구를 각각 복사할 수 있는 코드 블록으로 보여주도록 추가했습니다.
- 명령어별·사용자별 실행 횟수를 `state.json`에 저장하고 봇 소유자 전용 `/명령어통계`에서 확인하도록 추가했습니다.
- 커뮤니티 표본 확률을 사용해 유니크 퍼밀리어 잠재능력 두 줄을 추첨하는 `/퍼밀리어` 명령어를 추가했습니다.
- 두 번째 줄이 1% 확률로 유니크가 되는 더블 프라임 결과를 표시하도록 했습니다.
- `/퍼밀리어` 결과를 실제 GMS 퍼밀리어 카드 UI와 Sherbet 펫 외형을 사용한 이미지 한 장으로 표시하도록 했습니다.
- `/퍼밀리어` 임베드 제목을 `퍼밀리어 유니크 시뮬레이터`로, 카드 표시명을 `Sherbet`으로 변경했습니다.
- `/퍼밀리어` 카드 글꼴을 백묵 돋움으로 변경하고 유니크 테두리를 이름표와 같은 색으로 맞췄습니다.
- `/퍼밀리어` 임베드 아래의 확률 안내 문구를 제거했습니다.
- `/퍼밀리어` 카드의 Fusion Gauge 글자 겹침과 Lv 숫자 정렬을 수정했습니다.
- `/퍼밀리어` 카드 글꼴을 나눔고딕으로 되돌렸습니다.
- `/퍼밀리어` 임베드 제목을 제거하고 색상을 유니크 카드 색으로 맞췄습니다.

### 변경됨
- `/랭킹`의 지역 선택을 제거하고 북미 GMS만 조회하도록 단순화했습니다.
- `/랭킹` 결과를 최근 공식 랭킹 조회 기록에 따른 경험치 변화 이미지 그래프로 변경하고 기록을 `ranking.db`에 보관하도록 했습니다.

## 2026-08-13

### 추가됨
- GMS·KMS 헤어와 성형 이름을 영문명·한글명·ID로 찾는 `/외형검색` 명령어를 추가했습니다.
- 관리자용 `/서버알림`에 역할 선택을 추가해 서버 오픈 시 채널별 역할을 멘션하도록 했습니다.
- 정기점검 종료 1시간 전부터만 서버 상태를 확인하고, 종료 시각 없는 긴급점검은 공지 발견 직후부터 서버가 열릴 때까지 확인하도록 했습니다.
- 점검 일정과 감시 상태를 `state.json`에 저장해 봇 재시작 후에도 진행 중인 감시를 이어가도록 했습니다.
- 관리자용 `/환율기록알림`을 추가했습니다. 한국시간 날짜별 메시지 하나를 계속 수정하며 최근 환율 변동 5개만 표시합니다.
- 환율 기록과 Discord 메시지 ID를 `state.json`에 저장해 봇 재시작 후에도 같은 날 기록을 이어가도록 했습니다.
- 관리자용 `/정보채널`을 추가해 선택한 음성 채널에 한국시간을 5분마다 표시하도록 했습니다.
- 네이버 금융에서 USD/KRW 매매기준율을 크롤링해 선택한 음성 채널에 10분마다 표시하도록 했습니다.

### 변경됨
- 시간 음성 채널 갱신 주기를 5분에서 10분으로 조정해 Discord 채널 이름 변경 제한으로 반복되던 429 경고를 방지했습니다.
- `/5퍼`에서 스우·데미안·루시드·윌·더스크·진 힐라·듄켈을 `검밑`으로 묶어 7개 보스의 대표 난이도 5% 수치를 한 번에 확인하도록 변경했습니다.
- `/5퍼` 개별 보스 결과에서 제목과 중복되던 보스 이름을 본문에서 제거했습니다.
- `/채널추천`이 기존 말투와 비슷한 20가지 안내 문구 중 하나를 무작위로 보여주도록 변경했습니다.
- 서버가 열리면 알림을 한 번 보낸 뒤 다음 점검까지 서버 상태 API를 호출하지 않도록 변경했습니다.
- 진행 중인 점검 공지가 수정되면 변경된 종료 시각을 5분 안에 다시 저장하도록 했습니다.
- `/5퍼`의 난이도 자동완성이 Discord의 한글 옵션명 `보스`를 읽지 못해 빈 목록을 표시하던 문제를 수정했습니다.
- 환율이 바뀌지 않으면 기록을 추가하지 않고, 상승은 `🔴 ▲`, 하락은 `🔵 ▼`로 구분하도록 했습니다.
- 시간·환율 페이지 또는 Discord 채널 이름 변경에 실패해도 기존 채널 이름을 유지하고 봇의 다른 기능은 계속 동작하도록 했습니다.
- 시간 채널은 실행 시각의 분을 그대로 표시하지 않고 `00분`, `05분`처럼 직전 5분 단위로 표시하도록 했습니다.

## 2026-08-12

### 수정됨
- `/스스비` 임베드 하단의 공식 페이지 반올림 확률 안내 문구를 제거했습니다.
- `/캐샵` 임베드에서 링크와 업데이트 내용 사이에 빈 줄을 추가했습니다.
- `/스스비`에서 2.00% 이하 아이템 이름 앞에 보라색 표식을 표시하도록 변경했습니다.
- `/5퍼` 보스 선택 목록의 후반부를 칼로스·최초의 대적자·카링·찬란한 흉성·림보·발드릭스·유피테르 순서로 정렬했습니다.
- `/5퍼`에서 헬럭스 선택 후 난이도 목록이 비어 명령어를 실행할 수 없던 문제를 수정하고 `일반` 선택지를 표시하도록 변경했습니다.
- `/5퍼` 임베드 제목을 `익스트림 최초의 대적자 5%`처럼 난이도와 보스 이름을 띄어 표시하도록 변경했습니다.
- `/스스비`에서 3%·4% 아이템까지 보라색으로 표시되던 문제를 고쳐 2.00% 이하만 보라색 슬롯을 사용하도록 수정했습니다.
- `/스스비` 1회·5회 결과를 각각 메시지 한 개와 합성 이미지 한 장으로 표시하도록 변경했습니다.
- `/스스비` 5회 결과 이미지를 실제 GMS SSB처럼 다섯 슬롯의 가로 배열로 만들고, 2.00% 이하는 보라색 슬롯, 2.00% 초과는 회색 슬롯으로 표시하도록 했습니다.
- PSSB 합성 이미지에 GMS 클라이언트에서 추출한 광원·슬롯과 로컬 WZ 아이템 아이콘을 사용하도록 추가했습니다.
- `/심볼계산기`에서 엘라노스 20%를 기본 일일 보상에만 적용하고 보약 보너스는 별도로 더하도록 수정했습니다.
- 우르스 골든타임을 실제 고정 시간인 UTC 01:00~05:00와 18:00~22:00에 맞게 수정했습니다.
- `/5퍼` 결과 임베드 제목에 `하드루시드 5%`처럼 선택한 난이도와 보스가 표시되도록 변경했습니다.
- `/5퍼`에서 지원하는 보스 18종 모두 해당 이미지를 썸네일로 표시하도록 추가했습니다.
- `/5퍼` 결과에서 전투분석 보상 안내와 K 단위 설명 문구를 제거했습니다.

## 2026-08-11

### 추가됨
- GMS·KMS 게임 폴더에서 캐시 아이템 이름과 아이콘 DB를 다시 만드는 Windows용 `tools/update_cash_item_db.ps1`을 추가했습니다.
- GMS 캐시 아이템 44,341개의 영문명과 동일 ID가 있는 KMS 한글명을 검색하는 `/아이템검색` 명령어를 추가했습니다.
- 일반 캐시 장비 검색 결과에 WZ 데이터에서 추출한 아이콘을 함께 표시하도록 했습니다. 헤어·성형은 독립 아이콘이 없어 이름만 표시됩니다.
- 넥슨 공식 API로 Scania·Bera·Kronos·Hyperion의 현재 접속 상태를 보여주는 `/서버` 명령어를 추가했습니다.
- 관리자용 `/서버알림`을 추가하고 주요 4개 월드가 점검 중에서 모두 정상으로 바뀔 때 등록 채널에 한 번 알리도록 했습니다.
- 최신 공식 Cash Shop Update와 데이터 마이닝 링크를 썸네일과 함께 보여주는 `/캐샵` 명령어를 추가했습니다.

### 변경됨
- 우르스 골든타임 임베드 하단의 미국 서부시간 안내 문구를 제거했습니다.
- `/exp쿠폰`에서 시작 레벨을 먼저 입력하고, Lv.200~259에서는 일반 EXP 교환권만 선택 목록에 표시하도록 변경했습니다.
- `/캐샵`에 최신 공식 공지에서 새로 업데이트된 주요 항목을 최대 5개까지 함께 표시하도록 변경했습니다.
- `/5퍼`의 `말레픽 스타`를 공식 한국어 이름인 `찬란한 흉성`으로 수정하고, 선택한 보스에 존재하는 난이도만 자동완성에 표시하도록 변경했습니다.
- `/스스비` 5회 실행 결과를 각각 별도 메시지로 보내 다섯 결과가 모두 표시되도록 수정했습니다.
- 캐시 아이템 DB에 KMS 동일 ID가 없는 GMS 전용 아이템도 포함해 44,341개 이름과 14,847개 아이콘을 검색·PSSB 표시에 사용할 수 있도록 확장했습니다.
- `/아이템검색`이 KMS 동일 ID가 없는 GMS 아이템을 명확히 안내하도록 변경했습니다.
- PSSB 남녀 아이템 이름의 `(M)`·`(F)` 표시를 제거한 뒤 DB와 연결하도록 변경했습니다.
- `/스스비` 결과를 아이템별 임베드로 나누고 로컬 WZ 데이터베이스에서 정확히 연결되는 아이템의 아이콘을 함께 표시하도록 변경했습니다.
- 5분 공지 확인 작업이 최신 `sale` 카테고리의 Cash Shop Update 링크를 `state.json`에 자동 저장하도록 변경했습니다.

### 추가됨

- 글로벌 리부트 보스 18종, 난이도별 35개 총 체력과 보상 획득에 필요한 5% 최소 피해량을 보여주는 `/5퍼` 명령어를 추가했습니다.
- 미국 서부의 서머타임을 자동 적용해 현재 골든타임 여부를 보여주는 `/우르스`와 시작·종료 시각에만 보내는 관리자용 `/우르스알림`을 추가했습니다.

### 수정됨
- `/5퍼` 결과의 총 체력과 5% 최소 피해량을 인게임 전투분석과 같은 K 단위로 표시하도록 변경했습니다.

- `/캐시이동` 명령어와 당일 알림 임베드 하단에 캐시이동 캐릭터 이미지를 표시하도록 변경했습니다.
- `/성장의비약`에서 하이퍼 버닝과 비욘드 버닝을 생략하면 기본적으로 미적용하도록 변경했습니다.
- `/성장의비약`의 입력 순서를 비약 종류·시작 레벨·경험치·개수·하이퍼 버닝·비욘드 버닝 순으로 변경했습니다.
- 미라클 타임 안내의 사용 가능 큐브를 레드·블랙 큐브로 표기하고 대상 장비에 어깨장식을 추가했습니다.
- `/성장의비약` 결과 제목에 선택한 비약에 맞춰 EGP·UGP·MGP·TGP·LGP 커스텀 이모지를 표시하도록 변경했습니다.
- `/채널추천`의 안내 문구를 광휘와 칠흑 보상을 기원하는 말투로 변경했습니다.
- `/심볼계산기`의 보약 레벨과 엘라노스를 생략할 수 있게 하고, 사용자별 마지막 선택값을 `state.json`에 저장해 다음 계산에서 재사용하도록 했습니다.
- `/exp쿠폰`의 버닝 선택을 생략할 수 있게 하고, 사용자별 마지막 선택값을 `state.json`에 저장해 다음 계산에서 재사용하도록 했습니다.
- `/스스비` 시뮬레이터 결과의 제목 이모지를 SSB 커스텀 이모지로 변경했습니다.
- `/미라클큐브` 명령어와 당일 알림의 제목 이모지를 BC 커스텀 이모지로 변경했습니다.
- `/캐시이동` 명령어와 당일 알림의 제목 이모지를 ladyblair 커스텀 이모지로 변경했습니다.
- 아케인 `/심볼계산기` 결과에 지역별 주간퀘 120개를 이번 주에 수행한 경우와 수행하지 않은 경우의 예상 소요일·완료일을 함께 표시하도록 했습니다.
- 주간퀘 보상에는 엘라노스와 보약 보너스를 적용하지 않도록 했습니다.
- `/썬데이목록`에서 24시간이 지난 일정을 제외하고 `/썬데이`의 제목과 안내를 썬데이 메이플로 표기하도록 했습니다.
- `/도움말`을 일반 사용자용 `/명령어`로 변경하고 관리자 전용 알림 설정을 목록에서 제외했습니다.
- `/핫위크`와 `/큐브세일`에서 테스트용 안내 대신 현재 진행 중인 이벤트가 없다고 안내하도록 했습니다.
- `/익성비`의 기본 새싹 이모지를 EGP 커스텀 이모지로, `/에픽던전`의 검 이모지를 선택한 던전의 커스텀 이모지로 변경했습니다.
- `/익성비` 결과에서 EGP 엑셀 표를 기준으로 계산한다는 하단 문구를 제거했습니다.
- 썬데이 메이플 번역의 `Spiegelette`를 `슈피겔라`로, `Haste Fever Time Booster`를 `헤이스트 피버 타임 부스터`로 표시하도록 고정했습니다.
- `/성장의비약`의 하이퍼 버닝·비욘드 버닝 선택지를 `True/False` 대신 `적용/미적용`으로 표시하도록 했습니다.
- `/exp쿠폰` 결과 제목에 일반 교환권은 EV, 상급 교환권은 AEV 커스텀 이모지를 표시하도록 했습니다.

## 2026-08-10

### 수정됨

- 익스트림 성장의 비약 확률 중 기존 보간값과 EGP 엑셀 표가 달랐던 Lv.134·135·137·139·140 구간을 엑셀 기준으로 수정했습니다.
- `/심볼계산기`가 지역으로 심볼 종류를 자동 판별하고 현재 성장치 진행 상황을 표시하도록 개선했습니다.
- `/심볼계산기`에서 엘라노스 20% 추가 지급의 적용 여부를 선택하고 보약 보너스와 조합해 계산할 수 있도록 했습니다.

### 추가됨

- 일반 명령어와 관리자 전용 알림 설정을 기능별로 안내하는 `/도움말`을 추가했습니다.
- 아케인·어센틱·그랜드 어센틱 심볼의 필요 개수, 강화 메소와 일일 퀘스트 기준 완료일을 계산하는 `/심볼계산기` 명령어를 추가했습니다.
- 엘라노스 20% 추가 지급과 심볼 종류별 보약 1~6레벨 보너스를 2026년 9월 8일 23:59 UTC까지 적용하도록 했습니다.

## 2026-08-09

### 변경됨

- 하이마운틴·앵글러컴퍼니·악몽선경의 레벨별 경험치, 1.5·2·2.5배 보너스와 완료 후 레벨을 계산하는 `/에픽던전` 명령어를 추가했습니다.
- 선택한 에픽 던전의 입장 레벨을 검사하고 솔 에르다·조각 보상을 결과에 함께 표시하도록 했습니다.
- 실제 일정 수집 전 사용할 테스트용 `/큐브세일`과 관리자 전용 `/큐브세일알림` 채널 설정을 추가했습니다.
- 관리자 전용 `/캐시이동알림` 설정과 이벤트 시작 후 24시간 동안 채널별 한 번 보내는 당일 알림을 추가했습니다.
- 실제 일정 수집 전 사용할 테스트용 `/핫위크` 명령어를 추가했습니다.
- 일반 공지 임베드 하단의 넥슨 API 게시 시간을 제거해 Discord 실제 전송 시간과 혼동되지 않도록 했습니다.
- 사후 수정된 최신 패치노트에서도 Cash Shop Transfer와 Miracle Time 일정을 다시 읽어 `state.json`에 저장하도록 추가했습니다.
- 저장된 캐시이동 기간을 보여주는 `/캐시이동`과 날짜별 미라클 타임을 보여주는 `/미라클큐브` 명령어를 추가했습니다.
- 관리자 전용 `/미라클큐브알림` 설정과 각 일정의 UTC 00:00 당일 알림을 추가했습니다.
- Lv.130~199 익스트림 성장의 비약 결과를 무작위로 추첨하는 `/익성비` 명령어를 추가했습니다.
- 제공된 확률표와 5% 변화 패턴으로 추정한 누락 구간 확률을 저장하고, Lv.200 도달 시 남은 비약을 사용하지 않도록 구현했습니다.
- 레벨 200~299에서 익성비·궁성비·극성비·초성비·전성비의 적용 결과를 계산하는 `/성장의비약` 명령어를 추가했습니다.
- 제공받은 최신 표의 구간 초과 고정 경험치를 적용하고, 하이퍼 버닝은 Lv.260, 비욘드 버닝은 Lv.270에서 정확히 끝나도록 구현했습니다.
- 제공받은 최신 표와 하이퍼·비욘드 버닝을 적용해 일반·상급 EXP 교환권 사용 결과를 계산하는 `/exp쿠폰` 명령어를 추가했습니다.
- 고정 표 데이터를 `maple_data.py`, 순수 계산 함수를 `maple_calculators.py`로 분리해 봇 파일의 역할을 줄였습니다.
- 공식 PSSB 확률표를 읽어 1회 또는 5회 결과를 추첨하는 `/스스비` 명령어를 추가했습니다.
- 확률을 공유하는 남녀 아이템을 하나의 PSSB 보상으로 합쳐 추첨하도록 처리했습니다.
- 메이플스토리 게임 채널 1~40 중 하나를 무작위로 골라 주는 `/채널추천` 명령어를 추가했습니다.
- 관리자 전용 `/공지알림`, `/썬데이알림`, `/썬데이목록알림` 명령어로 여러 텍스트 채널의 알림을 ON/OFF할 수 있도록 추가했습니다.
- 저장된 최신 전체 일정을 보여주는 `/썬데이목록` 명령어를 추가했습니다.
- 기존 환경변수 채널을 최초 한 번 새 알리미 설정으로 이전하고 이후 설정을 `state.json`에 저장하도록 변경했습니다.
- Sunny Sunday 당일 메시지 ID를 채널별로 저장해 여러 채널에서 전송하고 24시간 뒤 각각 삭제하도록 변경했습니다.
- 목록 알림을 새 채널에서 ON하면 저장된 최신 목록을 한 번 보내고, 이후 새로운 패치노트 목록만 알리도록 변경했습니다.
- 한 번만 사용하던 요약 번역 함수를 제거하고 기존 번역 함수를 직접 호출하도록 단순화했습니다.

## 2026-08-05

### 변경됨

- `/썬데이` 명령어가 전체 잔여 목록 대신 현재 진행 중이거나 가장 가까운 다음 일정 한 건만 보여주도록 변경했습니다.
- 전용 채널 기능의 첫 배포 또는 채널 변경 시 저장된 Sunny Sunday 잔여 일정을 새 채널에 한 번 게시하도록 추가했습니다.
- Sunny Sunday 전체 일정과 주간 자동 알림을 `SUNNY_SUNDAY_CHANNEL_ID` 전용 채널에 보내도록 변경했습니다.
- Sunny Sunday 번역 결과와 주간 메시지 ID를 `state.json`에 저장하도록 변경했습니다.
- 일정 시작 시 해당 주 혜택을 공지 채널에 자동 전송하고 24시간 뒤 삭제하도록 추가했습니다.
- 저장된 일정 중 종료되지 않은 항목을 보여주는 `/썬데이` 슬래시 명령어를 추가했습니다.
- Sunny Sunday 안내를 날짜별 임베드 필드로 나누고, 만료되지 않도록 프로젝트에 저장한 배너 이미지를 직접 첨부합니다.
- 실제 업데이트 패치노트에서 Sunny Sunday 날짜와 혜택을 추출해 전용 한국어 임베드로 보내도록 추가했습니다.
- 반복되는 Sunny Sunday 혜택에는 고정 번역을 적용하고, 그 외 문구만 Google 번역을 사용하도록 했습니다.
- `/헥사` 명령어를 개인 Discord 계정에 설치해 서버와 DM에서도 사용할 수 있도록 허용했습니다.
- `/헥사` 결과에 HEXA, 솔 에르다, 솔 에르다 조각 애플리케이션 이모지를 추가했습니다.
- 6종류 HEXA 코어의 강화 재료를 계산하는 `/헥사` 슬래시 명령어를 추가했습니다.
- 이미지 표의 0→30 총합과 예시 구간을 검증하는 HEXA 비용 테스트를 추가했습니다.
- 공식 썸네일 이미지 CDN 주소를 수정해 Discord 임베드 이미지가 표시되도록 했습니다.
- 새 카테고리를 켤 때 기존 공지를 기준점으로 저장해 과거 공지 도배를 막도록 수정했습니다.
- 메이플스토리 공식 뉴스 API의 썸네일을 Discord 공지 임베드에 표시하도록 추가했습니다.
- 카테고리별 임베드 색상 코드 옆에 한글 색상 주석을 추가했습니다.
- `events` 공지 카테고리를 추가하고, 이벤트 임베드에 초록색을 적용했습니다.
- Discord 공지 임베드에 `MapleStory | 카테고리` 라벨과 카테고리별 색상을 추가했습니다.
- 새 공지 확인 주기를 10분에서 5분으로 줄였습니다.
- 새 공지 확인 주기를 30분에서 10분으로 줄였습니다.
- 공지 확인 전 대기 함수의 이름 충돌을 수정해 첫 공지 확인이 실행되도록 했습니다.
- 디스코드 연결 완료 뒤에 공지 확인을 시작하도록 수정했습니다.
- 공지 전송 성공과 처리 오류를 서버 로그에서 확인할 수 있게 했습니다.

이 파일에는 프로젝트를 수정할 때마다 사용자에게 영향을 주는 변경 사항을 기록합니다.

## 2026-08-04

### 변경됨

- GitHub에 API 키와 실행 기록이 올라가지 않도록 `.gitignore`를 추가했습니다.
- Git 이력에서 `.env`와 Python 캐시 파일을 제외하도록 정리했습니다.
- 전체 번역 명령어를 제거했습니다.
- 기본 알림 흐름을 "OpenAI 영어 요약 → Google 한국어 번역"으로 변경했습니다.
- 파파고 설정을 제거하고 Google Cloud Translation API 키 설정을 추가했습니다.
- README를 한글 설치·실행 안내서로 변경했습니다.
- 주요 코드 처리 단계에 쉬운 한국어 주석을 추가했습니다.

### 참고

- 새 기능 추가, 동작 변경, 버그 수정, 설정 변경이 있을 때 이 파일에 날짜와 내용을 추가합니다.
