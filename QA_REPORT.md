# 봇 품질 점검 — 2026-09-13

## 범위와 판정

홈페이지·외부 협업 확장 전, 기존 봇의 명령·자동 알림·수집·운영 복구를 점검합니다.
이번 결과는 자동 테스트, 공개 API 조회, 운영 서비스·로그 확인, 오류 주입에 근거합니다.
실제 Discord `에무새의 새장 / #test`에서 대표 명령 입력·버튼 조작·수신까지 확인했습니다.
관리자 설정은 현재 계정에서 명령 검색 결과가 나오지 않아 서버 설치·관리자 권한 확인 대기입니다.
따라서 전체 E2E 통과나 모든 기능 정상으로 판정하지 않습니다.

## 확인한 결과

- 변경 전 `python -m unittest discover -q`: 449개 통과.
- 수정 후 같은 전체 테스트: 450개 통과(48.038초), `git diff --check` 통과.
- 공개 API와 실제 파서: 뉴스 371건, 최신 Known Issues v.271, 서버 상태 4월드, 스스비 32항목, 시그니처 10항목, 원더베리 52항목 조회 성공. 확률 합계·구성품 이미지·Discord 렌더링까지 검증한 것은 아닙니다.
- 메인 Discord 봇, 메인·보조 3대 랭킹 서비스 모두 실행 중. 서버별 서비스 이름은 동일하지 않습니다.
- 보조 1번 공식 Discord 수집기는 `status=ok messages=10`과 최근 전송 로그 확인.
- 보조 2번 랭킹 결과 묶음의 최근 전송 로그 확인. 모든 샤드의 하루 전체 수집 완료·실제 누락 여부는 별도 검증이 필요합니다.
- 메인 디스크 45GB 중 72% 사용, 여유 13GB. 보조 3대 여유 각각 약 33/23/34GB.
- 관리자 권한·동일 서버 채널 여부·봇 채널 권한을 설정 시 검사하는 코드 확인. 실제 계정별 권한 E2E는 미실행.

## 우선순위별 발견 사항

### P1 — GPT 오류로 패치·Known Issues 감시 작업 종료 가능

- `check_patch_revisions`의 예외 처리에 OpenAI SDK 오류가 빠져 있었습니다.
- 실제 `discord.ext.tasks.loop`에 첫 회차 GPT 오류를 주입하면 다음 회차 전에 작업이 종료되는 것을 재현했습니다. 운영에서 실제 발생했다는 뜻은 아닙니다.
- 로컬 수정: `OpenAIError`를 잡아 다음 5분 주기에 저장된 미전송 변경분을 재시도합니다.
- 기존 테스트 파일에 실제 반복 작업 두 회차를 확인하는 테스트를 추가했습니다. 관련 25개 테스트 통과.
- 서버 배포·재시작·커밋·푸시는 이번 점검에서 수행하지 않았습니다.

### P2 — 90일 이전 랭킹 보관 기능의 운영 설정 미완료

- 운영 로그에 `ranking_archive ... reason=replica_not_configured`가 반복됩니다.
- 코드상 별도 복사 위치가 없으면 원본을 유지하고 보관 작업을 건너뜁니다. 당장 데이터 유실을 뜻하지 않지만 DB 증가를 제한하지 못합니다.
- 주간 전체 DB 백업과 별개입니다. 복사 저장소 구성·복원 검증·원본 정리 조건을 확인한 뒤 활성화해야 합니다.

### P3 — 삭제된 접두사 명령이 오류 로그를 생성

- 오늘 운영 로그에서 `!도핑` 입력에 대한 `CommandNotFound`를 확인했습니다.
- 사용자 요청대로 명령이 제거된 상태이며 장애로 볼 일은 아닙니다. 일반 미등록 명령과 실제 처리 오류를 로그에서 구분하는 보완이 필요합니다.

## 실제 Discord에서 이어서 확인할 순서

1. 신규 서버 설치: 초대 → 명령 표시 → 일반 사용자 설정 차단 → 관리자 알림 채널 지정 → 재접속 후 설정 유지.
2. 대표 명령: `/랭킹` 최근 10개·빈 입력·없는 닉네임·외부 조회 실패, 계산기 입력·선택·계산·타인 버튼 조작.
3. 시뮬레이터: 스스비·시그니처·원더베리 실행·재시도·이미지·영구 펫 배경·실제 구성과 확률.
4. 공지: 새 글·본문 수정·Known Issues 해결 이동 → 번역 → 설정 채널 수신 → 동일 내용 중복 방지.
5. 점검: 연장·채널 점검 완료에는 오픈 알림 없음, 명확한 Game is up에만 1회, 재시작·수정 후 중복 없음.
6. 이벤트: UTC 시작·종료 경계, 알림 OFF, 채널 권한 회수, 일부 채널 전송 실패 후 재시도.

알림·역할 멘션·서버 설정 변경 테스트는 지정된 테스트 서버에서 수행합니다.
현재 미검증: 전체 명령과 경계 입력, 번역의 원문 전수 대조, 신규 서버 온보딩, 관리자 설정 변경, 다른 계정의 버튼 접근 차단, 실제 점검·이벤트 자동 발송, 재시작 복구, 하루 수집 완결성, 동시 사용자 부하.

## 실제 Discord 대표 흐름 결과 (2026-09-13 23:00~23:12 UTC)

테스트 채널: https://discord.com/channels/697445093694767125/1548827474664620052
서버 설정·권한·알림 구독은 변경하지 않았고 테스트 메시지는 남겼습니다.

| 흐름 | 관측 결과 |
| --- | --- |
| `/명령어` | 한국어 명령 검색 및 본인 전용 도움말 응답 성공 |
| `/exp-coupon` | 영문 검색이 한국어 명령으로 연결, 추가 인자 없이 패널 열림 |
| EXP 잘못된 입력 | 레벨 199·쿠폰 10개 입력 거절, 허용 범위 안내, 다시 입력 가능 |
| EXP 정상 입력 | 레벨 260·0%·10개 입력 → 계산 활성 → 765,720,000 경험치·0.044% 표시 |
| `/시그니처` 5회 | 결과 5개·39,500 NX 표시 |
| 시그니처 재추첨 | 같은 메시지 수정, 누적 10회·79,000 NX 표시 |
| 시그니처 기대값 버튼 | 본인 전용 확률·비용 안내 응답. 비용 계산 정밀도는 아래 보완 사항 참고 |
| `/원더베리` 5회 | 펫 5개·기간·확률·20,000 NX 표시, 스크린샷에서 정적 배경과 아이콘 확인 |
| 빈 `/랭킹` 첫 사용 | 닉네임을 먼저 입력하라는 본인 전용 안내 |
| `/랭킹 닉네임:Home` | 랭킹 카드 생성, 레벨·월드·경험치 그래프 표시. 캐릭터 초상은 비어 있음 |
| `/ranking` 최근 목록 | 영문 검색 성공, 닉네임 선택 시 방금 조회한 Home 표시 |
| `/알려진이슈` | v.271 심볼 선택권 설명의 x1/x2 오류에 대한 한국어 안내와 공식 링크 응답 |
| `/캐샵일정` | 진행 중·예정 기간 표시, 상품 목록 없이 일정과 변경 가능성 안내 |
| `/알림설정확인` | 일반 명령과 달리 검색 결과 없음. 봇 서버 설치 여부·계정 권한은 확인 전이며 원인 단정 불가 |

영구 펫이 이번 표본에 나오지 않아 보라색 배경은 실제 Discord에서 미검증입니다.
최근 목록 최대 10개·다른 사용자 분리·조회 후 빈 명령 기본값은 이번 UI 실행으로 모두 확인한 것은 아닙니다.

### 추가 보완 후보

- 랭킹 초상: 최초 Home 테스트에서만 빈 영역을 관측했고 사용자는 같은 조회에서 정상 표시됐다고 확인했습니다. 반복 재현된 결함으로 분류하지 않습니다. 일시적인 이미지 조회·표시 실패 가능성은 있으나 원인은 미확정입니다.
- 기대값 비용: 시그니처 7%에서 110,600 NX, 30%에서 23,700 NX로 표시됐습니다. 코드가 `round(1 / p)`로 횟수를 먼저 반올림한 뒤 구매 비용을 계산합니다. 단가 7,900 NX의 정확한 기대 비용은 각각 약 112,857 NX, 26,333 NX이므로 평균 비용이라는 표현에 맞게 계산하거나 근사 기준을 명확히 해야 합니다. 같은 방식이 스스비·원더베리에도 있어 묶음 구매 정책을 함께 정해야 합니다.

### 사용자 피드백 반영

- 시그니처 기대 비용은 사용자 요청으로 로컬 수정했습니다. 7%·30%·100% 비용 회귀 테스트와 시뮬레이터 관련 24개 테스트가 통과했습니다. 스스비·원더베리는 묶음 할인이 있어 같은 단가 나눗셈을 적용하지 않았으며, 해당 기대 비용 보완 후보는 남아 있습니다. 서버 미적용입니다.

- 캐샵 일정은 날짜 렌더링만 확인하고 사용성까지 정상으로 분류한 것이 부족했습니다. 무엇의 일정인지 식별할 정보가 없어 로컬 데이터에 원본 기반 판매 종류를 복원했습니다. 관련 테스트 2개 통과, 서버 미적용입니다.
- 사용자가 관리자 권한을 부여했다고 알려줘 Discord 클라이언트를 새로고침해 재확인했습니다. `/알림설정확인`과 `/news-alert` 모두 검색 결과가 나타나지 않았습니다. 권한 부여 대상을 단정하지 않으며 서버 설치·명령 권한 원인은 추가 확인이 필요합니다.
